# AI Job Agent 技术决策记录

## 岗位JD分析输出格式

- 问题：大模型自由文本不稳定，无法可靠参与岗位匹配计算。
- 选择：使用Pydantic `JobRequirementResult`约束Structured Output。
- 备选方案：直接返回自然语言或手动解析普通JSON。
- 为什么选择：字段稳定、自动验证、便于前端展示和后续评分。
- 缺点：修改输出字段时，需要同步调整后端Schema和前端显示。
- 未来升级：增加Timeout、Retry、Token成本记录和Redis缓存。

## AI测试模式

- 问题：开发期间重复调用真实模型会产生费用，并受网络波动影响。
- 选择：通过`LLM_MOCK_MODE`在模拟结果和真实模型之间切换。
- 备选方案：每次都调用真实模型。
- 为什么选择：测试速度快、结果稳定、不会产生Token费用。
- 缺点：模拟结果不能证明真实模型效果。
- 未来升级：上线前关闭Mock，并建立真实岗位评测集。

## 多来源技能如何计分

- 问题：数据库技能和AI提取的必备技能可能重复，分别加分会让总分虚高。
- 选择：先将两个来源统一小写、去空格并去重，再作为一个35分技能维度计算。
- 备选方案：两个来源分别计分，或只采用AI结果。
- 为什么选择：保留人工数据与AI补充能力，同时避免同一技能重复计分。
- 缺点：AI误提取的技能可能扩大分母，需要通过岗位测试集继续校准。
- 未来升级：加入技能别名表和评测数据，记录调整前后的匹配结果。

## 职责匹配使用什么简历数据

- 问题：原简历分析只有总结和技能，没有可供职责匹配的结构化工作经历。
- 选择：新增 `work_experience` JSON列表，并通过数据库迁移保留已有记录。
- 备选方案：直接拿总结文本评分，或每次重新解析整份PDF。
- 为什么选择：经历数据可验证、可保存、可重复使用，也更容易展示匹配证据。
- 缺点：旧分析记录的经历默认是空列表，需要重新分析简历才能得到真实内容。
- 未来升级：增加项目经历和教育字段，并使用正式迁移工具管理MySQL结构。

## 第一版职责匹配为什么先使用关键词规则

- 问题：需要把岗位职责和简历经历进行匹配，并向用户展示可以核对的证据。
- 选择：第一版使用共享关键词寻找简历证据，按命中职责比例计算最高30分。
- 备选方案：直接让大模型给分，或立即引入Embedding语义匹配。
- 为什么选择：规则透明、成本为零、结果稳定、容易自动化测试，适合作为后续语义匹配的基线。
- 缺点：无法识别不同措辞表达的相同含义；过宽关键词可能产生误匹配。
- 约束：不把“开发”等通用词加入关键词表，避免为了提高分数制造虚假匹配。
- 未来升级：建立真实职责/经历测试集，再增加Embedding语义分并与规则分组合。

## 岗位方向为什么单独计分

- 问题：技能相似不等于岗位方向一致，例如会Python的人不一定想应聘所有Python相关岗位。
- 选择：把岗位名称与AI推荐岗位方向进行规则比对，方向一致时获得10分，并返回匹配原因。
- 备选方案：只使用技能分，或让大模型直接判断岗位方向。
- 为什么选择：规则透明、没有模型费用、容易测试，并能向用户解释推荐原因。
- 缺点：当前关键词表较少，无法识别所有岗位别名。
- 未来升级：根据真实岗位测试集补充岗位别名，并加入Embedding语义匹配。

## 求职偏好如何计入规则分

- 问题：技能和经历相符的岗位，仍可能因为城市或薪资不合适而不值得推荐。
- 选择：城市最高5分、薪资最高10分；岗位最高薪资达到用户最低要求时判定薪资符合。
- 备选方案：偏好作为硬过滤条件，或交给大模型判断。
- 为什么选择：第一版保留所有岗位供用户比较，同时用分数清楚表达偏好差异，规则无需模型成本。
- 缺点：当前只解析常见的`15-20K`格式，“面议”、日薪和年薪需要额外规则。
- 未来升级：建立真实薪资格式测试集，并根据用户反馈决定哪些条件改为硬过滤。

## 为什么在Embedding之前建立固定评测集

- 问题：加入语义匹配后，分数变化不代表推荐质量一定提高，甚至可能破坏已有正确排名。
- 选择：先固定一份模拟用户和3个代表岗位，记录规则基线分数与人工期望排名。
- 备选方案：只凭页面观感判断，或上线后再收集反馈。
- 为什么选择：结果可重复、修改前后可比较、可以进入自动化回归测试，也能形成面试证据。
- 缺点：当前只有3个案例，无法代表真实招聘市场的全部情况。
- 未来升级：从脱敏真实岗位中逐步增加正确、错误和边界案例，并记录准确率和排序指标。

## 第一版中文语义匹配使用什么模型

- 问题：关键词规则无法识别“开发FastAPI接口”和“构建Python REST API”这类措辞不同但含义接近的文本。
- 选择：使用本地`BAAI/bge-small-zh-v1.5`，通过Sentence Transformers生成归一化向量并计算相似度。
- 备选方案：调用外部Embedding API，或直接让LLM判断相似度。
- 为什么选择：中文效果适合当前场景、模型较小、可在CPU运行、结果可重复，并且开发测试不产生Token费用。
- 缺点：首次加载约十几秒并占用本机内存；相似度不是百分制，也不能未经评测直接设置固定合格线。
- 当前约束：语义结果先作为参考值返回，不直接加入现有100分规则总分，避免重复计分和未经校准的权重。
- 未来升级：扩充脱敏真实岗位评测集，比较加入语义层前后的排名，再决定阈值、权重和是否使用向量数据库。

## 如何管理AI慢测试

- 问题：本地Embedding模型使完整测试耗时147秒，严重影响日常代码修改后的反馈速度。
- 选择：使用`@pytest.mark.slow`标记模型测试；日常运行`not slow`快速测试，发布前再运行完整测试。
- 备选方案：每次都加载真实模型，或完全删除语义自动测试。
- 为什么选择：保留真实模型质量证据，同时把日常测试时间降低到约6秒。
- 缺点：开发者可能忘记在发布前执行慢测试。
- 未来升级：在CI中分别建立快速测试任务和完整AI测试任务，并缓存模型文件。

## 本地Embedding模型是否允许启动时联网

- 问题：模型已经缓存到本机，但默认加载仍会联网检查文件；断网时会反复重试，导致接口和测试等待很久。
- 选择：当前开发环境使用`local_files_only=True`，只读取本地模型文件。
- 备选方案：保持默认联网检查，或每次调用外部Embedding API。
- 为什么选择：当前模型已经下载，本地加载更稳定、速度更快，也不会依赖外网状态。
- 缺点：新电脑或新部署环境如果没有提前下载模型，会直接提示找不到模型。
- 未来升级：Docker镜像构建阶段预下载模型，并通过环境变量决定是否启用离线模式。

## Embedding如何进入岗位推荐

- 问题：语义相似度不是百分制，直接加进100分规则总分可能重复计算技能和经历，也没有可靠权重依据。
- 选择：第一版把语义值作为参考和排序证据，保留规则总分不变；固定评测集要求Python、Vue、销售岗位的语义排名为1、2、3。
- 评测结果：三个岗位语义分依次为0.884、0.646、0.335，符合人工期望顺序。
- 备选方案：直接把相似度乘权重加入总分，或完全交给LLM排序。
- 为什么选择：保留可解释规则基线，并用可重复测试证明语义层有效，再决定融合方式。
- 缺点：当前只有3个模拟岗位，不能代表真实招聘数据。
- 未来升级：增加脱敏真实岗位、错误案例和边界案例，再评估Top-K准确率与融合权重。

## LLM为什么只负责解释而不修改规则分数

- 问题：如果让LLM直接给岗位打分，同一份数据可能因模型波动得到不同分数，也难以解释每一分的来源。
- 选择：规则层计算100分正式总分，Embedding提供语义参考值，LLM只根据已有分数、匹配证据和能力缺口生成结构化中文解释。
- 备选方案：让LLM直接输出最终分数，或完全不使用LLM解释。
- 为什么选择：正式分数稳定、可测试、成本可控，同时保留LLM擅长的自然语言总结能力。
- 安全约束：Prompt要求不得修改分数、不得编造经历，并把输入资料包裹为待分析数据；返回值使用`MatchExplain`进行结构验证。
- 缺点：LLM解释仍可能表达不准确，外部API也可能超时或失败。
- 未来升级：增加Timeout、Retry、异常降级、Token成本记录和真实模型评测集。

## 外部LLM故障时如何保证岗位匹配可用

- 问题：LLM可能因为网络、超时、额度、限流或返回格式错误而失败，不能让这些故障导致整个岗位匹配接口返回500。
- 选择：共享客户端统一设置30秒超时和最多2次重试；岗位解释在重试仍失败后记录异常，并降级为规则证据生成的结构化解释。
- 备选方案：直接把异常返回给用户，或无限等待模型恢复。
- 为什么选择：规则评分是本地稳定能力，即使外部模型不可用，用户仍能看到分数、理由、缺口和行动建议。
- 测试方式：使用pytest `monkeypatch`替换LLM客户端并主动抛出异常，不依赖真实密钥、网络和模型额度。
- 缺点：当前使用`Exception`作为第一版外部边界，会同时捕获部分程序错误，必须依靠异常日志排查。
- 未来升级：根据真实错误类型缩小捕获范围，增加限流处理、结构化日志、告警和降级次数监控。

## AI简历分析失败时如何处理

- 问题：外部模型可能连接失败、请求失败或返回空结果。
- 选择：返回ai_ok=False的安全结果，不让接口崩溃，也不保存到数据库。
- 原因：用户仍能继续使用系统，同时避免失败数据污染正式分析记录。
- 缺点：前端需要明确提示用户稍后重试。
- 未来升级：记录失败次数并增加监控告警。

## 如何统计LLM调用成本

- 问题：真实模型按Token收费，不记录用量就无法评估上线成本。
- 选择：读取LLM响应中的usage，按照输入和输出单价分别计算费用，并写入日志。
- 为什么选择：实现简单，不增加数据库复杂度，适合当前第一版。
- 缺点：日志适合排错，但暂时不能直接统计每天和每个用户的总成本。
- 未来升级：把用量保存到数据库，并按用户、模型和日期汇总。

## RAG第一版为什么先使用本地Embedding和内存检索

- 问题：项目需要完成RAG完整数据路线，但当前资料量较小，暂时没有引入独立向量数据库的真实压力。
- 选择：使用本地中文Embedding模型生成向量，在内存中计算相似度并选取相关片段，再交给LLM生成结构化`RagAnswer`。
- 备选方案：直接引入FAISS、Chroma、pgvector或Milvus等向量存储方案。
- 为什么选择：实现简单、无额外服务成本，能够先学清楚“切分—向量化—检索—上下文—生成”的核心路线。
- 安全与可靠性：Prompt把参考资料标记为数据并禁止执行其中指令；无资料、外部请求失败或空结果时统一降级。
- 缺点：每次查询都会重新计算候选资料向量，数据不能持久化，不适合大量文档和高并发请求。
- 未来升级：接入真实知识库后缓存Embedding，并根据数据规模选择带元数据过滤的向量存储。

## RAG问答接口为什么要求JWT和后端输入验证

- 问题：Embedding和外部LLM会消耗计算资源与真实费用，公开接口容易被匿名滥用；前端验证也可以被绕过。
- 选择：使用`POST /rag/ask`接收只包含问题的`RagAsk`，通过`Depends(get_current_user)`要求JWT认证，并在Pydantic层限制问题长度、拒绝额外字段。
- 备选方案：提供匿名接口，或者只在Vue前端限制输入。
- 为什么选择：能够识别调用用户、尽早拒绝无效请求，并为后续限流、用户成本统计和知识库数据隔离打基础。
- 缺点：当前第一版使用服务端共享文本文件，还没有用户级知识库和持久化向量索引。
- 未来升级：资料改为从当前用户有权限访问的知识库检索，并增加Rate Limit、调用审计和按用户计费统计。

## RAG前端为什么复用Axios实例并保留后端JWT验证

- 问题：RAG页面既需要自动携带登录Token，又需要比普通接口更长的等待时间；仅靠Vue路由保护仍可能被绕过并直接请求后端。
- 选择：复用项目的Axios实例和请求拦截器自动添加JWT，只为`/rag/ask`单独设置30秒超时；Vue路由使用`requiresAuth`改善页面体验，FastAPI继续强制验证JWT。
- 备选方案：每个页面手工添加Token、把所有请求全局改为30秒，或者只依赖前端路由拦截。
- 为什么选择：减少重复认证代码，普通接口仍能快速失败，同时保证绕过前端页面也不能匿名调用消耗计算资源的RAG接口。
- 缺点：当前资料来自服务端共享文本文件，尚未记录独立来源元数据和用户归属。
- 未来升级：从用户有权访问的持久化知识库检索资料，并增加精确来源、请求取消、限流和按用户成本统计。

## RAG为什么由后端管理资料并增加最低相关分

- 问题：客户端可以伪造或批量提交候选资料；初版检索又会无条件采用Top-K结果，导致无关问题也被标记为资料足够。
- 选择：Vue只发送问题，后端读取受控知识文件；Schema使用`extra='forbid'`拒绝额外资料字段；统一使用`MIN_SCORE=0.6`过滤弱相关结果。
- 备选方案：继续信任客户端资料、只使用Top-K不设阈值，或者立即引入独立向量数据库。
- 为什么选择：先建立可信资料边界和最小可测检索质量控制，不提前增加向量数据库运维复杂度。
- 测试依据：正确资料最低最高分为0.6969，弱相关资料最高分为0.5616，无关问题最高分为0.2503；0.6能够分开当前评测集中的正确与错误资料。
- 缺点：0.6来自当前3条知识资料和4个评测问题，知识库扩大或更换Embedding模型后需要重新评测。
- 未来升级：扩大评测问题集，分别调优Top-K和阈值，保留来源元数据与分数，再根据数据规模选择向量存储。

## Agent第一版为什么使用单轮Tool Calling和白名单分发

- 问题：模型需要使用项目知识库，但模型输出不能直接当作可信代码执行。
- 选择：使用兼容OpenAI Chat Completions的Tool Calling，由模型选择`find_kb`，后端通过白名单分发并用`tool`消息返回结果；第一版最多执行一轮工具。
- 备选方案：把检索固定写进普通RAG流程，或者直接引入复杂的多轮Agent框架。
- 为什么选择：能够真实展示Agent的工具选择和执行闭环，同时保持代码简单、可测试和可解释。
- 安全设计：只允许明确注册的工具，未知工具立即抛出异常；工具参数使用JSON Schema严格约束。
- 缺点：第二次模型响应如果继续请求工具，当前版本不会再次执行。
- 未来升级：根据真实任务需要增加有限轮次循环、超时、调用次数限制、日志和用户成本统计。

## Agent为什么只在1305错误时切换备用模型

- 问题：免费主模型可能因为访问量过大返回`1305`，导致Agent暂时不可用；但认证、参数和程序错误不能通过盲目换模型掩盖。
- 选择：把单次请求封装为`call_model`；`ask_model`只捕获`RateLimitError`，并仅在错误码为`1305`且配置了备用模型时重试一次。
- 备选方案：所有异常都切备用模型、完全依赖SDK重试，或者直接把主模型错误返回用户。
- 为什么选择：模型拥堵通常可由兼容备用模型恢复；其他错误应尽快暴露，避免重复请求、额外成本和错误定位困难。
- 测试依据：覆盖主模型繁忙后切换成功、非`1305`不切换、没有备用模型不切换；Agent模块13项和后端61项测试通过。
- 真实验证：备用模型能够完成`find_kb` Tool Calling并基于知识库生成Docker回答。
- 缺点：当前只识别供应商错误码`1305`，模型策略或错误协议变化后需要同步调整。
- 未来升级：统一LLM适配层，按供应商映射可恢复错误，并增加切换次数、失败率、延迟和成本监控。

## 结构化LLM调用为什么集中到统一适配层

- 问题：简历、岗位、匹配和RAG服务分别创建客户端并使用旧的结构化解析方式，模型接口、超时、返回格式和异常处理容易重复且不一致。
- 选择：由`llm_service.py`统一创建兼容客户端；`build_json_messages`把Pydantic Schema加入系统消息；`call_structured`统一使用Chat Completions的JSON对象响应并执行Pydantic验证。
- 备选方案：每个业务Service继续独立调用模型，或者继续依赖供应商不兼容的`responses.parse`。
- 为什么选择：当前智谱兼容Chat Completions；统一适配层让业务Service只负责Prompt和结果Schema，后续切换模型地址、超时、重试和结构化策略时只修改一处。
- 安全与可靠性：空模型正文立即抛出`RuntimeError`；合法JSON仍需通过`model_validate_json`检查必填字段和类型，避免错误数据进入业务逻辑。
- 测试依据：使用假客户端验证消息、模型名称、响应格式、正常解析、空内容和缺字段错误；岗位迁移专项测试共5项、后端完整回归66项通过。
- 缺点：JSON对象模式不能单独保证业务字段正确；当前统一函数尚未集中处理主备模型切换、成本记录和所有供应商错误。
- 未来升级：完成其余结构化Service迁移后，再根据共同错误模式加入统一异常分类、主备策略和用量记录，避免提前过度设计。

## 结构化LLM为什么统一处理繁忙切换和Token字段兼容

- 问题：免费主模型可能返回`1305`繁忙错误；从Responses迁移到Chat Completions后，用量字段又从`input_tokens/output_tokens`变为`prompt_tokens/completion_tokens`。如果每个业务Service分别处理，容易遗漏并产生不一致。
- 选择：在`call_structured`中仅针对`RateLimitError`且错误码为`1305`时切换一次备用模型；在`read_use`中把两套第三方字段统一转换为项目内部的`TokenUse`。
- 备选方案：每个业务Service各自重试和转换字段，或者只支持当前一种接口字段。
- 为什么选择：主备策略和第三方字段差异属于公共基础能力，集中处理能减少重复代码；业务Service只需要负责Prompt、Schema和失败后的业务降级。
- 测试依据：覆盖繁忙后备用成功、非`1305`不切换、无备用模型不切换、Chat Completions用量字段转换；后端完整回归71项通过。
- 真实验证：简历分析遇到主模型繁忙后由备用模型成功返回结构化结果，Token读取未再崩溃。
- 缺点：当前只识别智谱错误码`1305`，主备模型最多切换一次；成本单价仍使用项目统一配置，尚未按实际模型分别计价。
- 未来升级：积累真实故障数据后建立可恢复错误映射，并记录实际响应模型、切换次数、延迟和按模型区分的Token成本。

## 岗位匹配为什么由模型负责语言、后端负责事实

- 问题：真实评测中，模型会把求职者的“Docker能力缺口”扩大为“完全缺乏Docker技能和经验”，还可能站在招聘方视角提出评估建议；合法JSON和正确Schema不能保证事实可信。
- 选择：规则和语义算法决定分数、匹配证据与能力缺口；模型只生成`summary`和`actions`，返回后由后端使用真实输入覆盖`reasons`和`gaps`。
- 备选方案：继续增加Prompt约束并完全相信模型字段，或者完全不用模型、全部采用固定模板。
- 为什么选择：Prompt可以降低错误概率但不能保证模型遵守；后端覆盖能够提供稳定、可测试的事实底线，同时保留模型生成自然语言总结和个性化行动建议的价值。
- 测试依据：假模型故意返回编造理由和缺口，测试确认最终结果被真实输入覆盖；岗位匹配专项9项、后端完整回归72项通过。
- 真实验证：固定评测案例最终返回`reasons=['Python','FastAPI']`和`gaps=['Docker']`，行动建议改为求职者学习和实践Docker。
- 缺点：可信`reasons`目前是简短规则证据，不如模型长句自然；`summary/actions`仍可能出现质量波动。
- 未来升级：前端或后端使用确定性模板美化可信证据；建立岗位匹配评测集，持续记录模型总结和行动建议的错误案例。

## RAG迁移统一调用后为什么仍由后端覆盖来源

- 问题：统一`call_structured`能够保证模型返回合法`RagAnswer`，但合法的`sources`文字和分数仍可能由模型编造，不能作为检索证据。
- 选择：模型生成`answer`并返回结构化结果；后端把结果中的`sources`强制替换为`pick_rows`产生的`RagSrc`，资料文字和相似度分数都来自真实向量检索。
- 备选方案：完全相信模型返回的来源，或把来源从模型Schema中删除后再由接口层额外拼装。
- 为什么选择：保留现有API数据结构且改动最小，同时确保前端展示的来源和分数可追溯、可复现、可测试。
- 测试依据：假模型故意返回“模型编造的来源”和0.1分，最终结果仍为Docker资料和0.9分；RAG专项16项、后端完整回归72项通过。
- 真实验证：Docker问题召回唯一资料，分数0.7819，FastAPI无关资料被过滤，最终回答与来源一致。
- 缺点：模型仍参与`enough`判断和回答生成；有相关资料不一定代表资料完整，仍需扩大评测集观察错误拒答和错误回答。
- 未来升级：根据真实问答记录评测`enough`准确率，并把来源元数据、文档编号和片段编号持久化，支持更精确的追溯。

## 手动JD辅助为什么不直接用语义相似度证明简历事实

- 问题：固定技术词表无法覆盖产品岗位的表达、协作和逻辑拆解能力；但直接使用子串或语义相似度验证证据，又可能把相关文字误判为真实经历。
- 选择：第一版继续用标准技术词进行确定性评分；模型生成的改写建议必须引用后端提供的完整经历原文；没有匹配技能时使用固定安全招呼语，不让模型自由描述候选人能力。
- 备选方案：把AI提取的长句直接并入技能列表、允许任意子串作为证据，或者只要语义相似就认可为真实经历。
- 为什么选择：技能匹配影响分数，简历证据影响诚信；宁可第一版少给建议，也不能生成求职者没有做过的经历。语义相关只能帮助召回候选证据，不能单独证明事实成立。
- 测试依据：覆盖规则评分、模型假证据过滤、摘要不能作为证据、空技能不创建模型客户端、接口认证与用户简历隔离；完整后端90项测试通过。
- 缺点：非技术能力暂时不进入匹配技能列表，严格原文相等会过滤模型的合理改写，部分岗位看起来分数偏低或建议为空。
- 未来升级：技术技能增加同义词标准化；通用能力作为独立维度使用语义检索召回；模型只能返回证据编号，由后端根据编号取回完整原文并展示给用户确认。

## 2026-09-03 编号引用解决出处问题，不等于完成事实核验

- 问题：要求模型逐字抄写经历会因文字变化而过滤建议；改为编号后，真实页面仍出现“引用新媒体运营经历，却补写前后端维护职责”的失败案例。
- 选择：AI返回`TailorDraft`，后端检查`proof_id`范围并取回原经历文字，转换为原有`TailorResult`；无有效引用的建议不保留，并将对应要求加入待补依据列表。前端展示引用、待补依据和明确的草稿风险提示。
- 备选方案：继续要求逐字抄写、采用子串或相似度作为事实证明、增加第二个模型审核、完全采用只展示原文的确定性方案。本轮没有实现后三种方案。
- 为什么选择：编号方式能稳定取回本次输入中的引用，不要求模型重复抄写文字，并保持前端接口结构不变。它仅解决可追溯性；相似度和引用编号都不能单独证明改写忠实于事实。
- 当前证据：用户运行的后端90项回归、前端92模块构建、固定FastAPI/Docker样例通过相应检查；真实运营岗位样例证明语义真实性仍不可靠，不能宣布防编造目标完成。
- 明确边界：编号有效不代表该经历支持对应岗位要求，也不代表`summary/rewrite`没有新增事实。经历文字来自当前简历分析列表，可能已经经过AI提取，不能直接当作经独立核实的原PDF证据；待补依据也不等于候选人一定没有该能力。
- 缺点：人工核对提醒不是自动事实校验，也不是强制确认流程；提示词约束不能保证遵守。当前没有自动消除无依据的改写。
- 文档用语：把此功能称为“编号引用与待核对草稿”，不称为“自动生成可信简历”或“已解决幻觉”。先前记录中的“可信证据过滤”应理解为对指定来源的引用检查，而非完整语义事实验证。
- 未来升级条件：以真实失败案例建立评测，在改变默认输出策略或增加模型审核前明确可靠性、延迟和费用取舍；必要时提供仅展示原文的保守路径和明确的用户确认流程。技术技能标准化与通用能力匹配是独立问题，不由此次编号迁移解决。

## 2026-09-03 技术技能只合并明确别名

- 问题：简历中的“Python编程”和岗位中的“Python”名称不同，直接比较会漏匹配。
- 选择：新增`skill_name`，先去两边空白、转小写，再查询小型明确别名表；查不到时保留清理后的文字。当前仅接入`calculate_skill_score`的两侧集合构造。
- 备选方案：删除全部中文、做任意子串匹配、引入语义模型或让LLM直接判定。当前不采用，避免把“正在学习Python”或“不会Docker”等描述转成无条件技能命中，也不为简单名称转换增加模型成本。
- 测试依据：新增别名命中及学习/否定表达不误命中两个用例；用户报告匹配文件13项、完整后端92项通过。此结果仅证明当前测试覆盖的行为。
- 缺点：别名需人工维护，未知名称仍可能漏匹配；不是通用语义理解，不证明真实能力。其他评分函数的名称转换口径、重复别名和更多空白边界尚待后续按实际需求检查。
- 未来升级：先记录真实漏匹配例子并补回归测试，再扩展明确映射；通用能力另作评测，不以名称或语义相似作为简历事实核验。
- 同日实施补充：已由用户将公共转换接入关键词评分、必备技能对照和岗位技能合并；新增4个回归测试，完整后端96项通过。合并前先统一名称再去重，避免同一别名要求重复计数；原分值权重与必备技能参考不计入总分的规则保持不变，页面实测仍待完成。

## 2026-09-03 暂停自由改写而非宣称通过事实核验

- 问题：真实模型反复把短运营经历扩写为未经引用支持的AI架构和模型经验，仅增加提示或展示引用编号没有阻止此类输出。
- 选择：模型条目格式移除`rewrite`，提示词不再要求改写；后端保留公共返回字段但固定置空，前端仅显示岗位要求和待核对候选经历。
- 为什么：直接限制这一字段的业务输出，减少用户误用风险；保留原接口形状以缩小改动，不新增模型审核费用。
- 备选方案：仅隐藏页面文字、继续叠加Prompt约束、增加第二个模型审核。仅隐藏仍会在接口泄露改写；Prompt无确定性保证；二次模型审核有成本和误判，需要评测后再考虑。
- 代价与边界：暂时失去自由润色功能；仍有模型总结、招呼语、候选对应关系及上游AI提取内容需要核对，本轮不声称这些内容已保证真实。
- 验证：用户后端98项通过；测试故意让假模型携带无依据改写，确认业务输出为空；前端构建通过，随后用户真实截图确认候选引用保留且自由改写段落消失。仅凭项目标题或运营岗位描述仍不能支持模型关联的复杂技术要求，不计为事实核验通过。
- 恢复条件：先积累包含正常、无依据和否定描述的真实案例，明确事实来源与用户确认方式，再按可靠性和费用权衡是否恢复生成。

## 2026-09-03 评分明细由后端同一次规则计算返回

- 问题：定制页只展示百分制总分，无法判断技能为空时的43分来自哪里。
- 选择：使用用户命名的`Scoreminxi`记录技能、经历、方向三项；分项必填并限制范围，随原响应返回，前端只展示，不重复计算或调用AI。
- 为什么：保持原规则与总分不变，保证明细来自同一次计算；避免前端重算造成口径分歧，避免默认零值掩盖漏传。自定义类名可变，但测试与业务引用必须一致。
- 验证：新增分项范围边界与传递断言；真实零分暴露`gt`与`ge`差异并由用户修正。完整后端105项通过，前端构建及0+22+10换算43的页面通过。
- 限制：明细解释计算来源，不证明评分合理。离线反例已证实共享AI导致经历与方向误判；后续依据案例改善规则，不让LLM直接决定分数，也不把相似度当成事实证明。

## 2026-09-03 经历匹配取消宽泛词独立加分

- 问题：双方仅共享AI或接口等宽泛词，内容运营经历也被当作技术职责命中。
- 选择：经历词表暂只复用现有`JOB_KEYWORDS`中的8个技术名称；保留原匹配流程、分母和30分权重，避免同时更换算法与评分口径。
- 备选：降低总分无法修复错误引用；再加模型判断需要额外成本与评测；要求一条经历覆盖所有技术词可能误伤岗位中的可选技术。本轮均不引入。
- 验证：新增12个正常、误判及边界案例；旧规则7失败/23通过，进程内模拟新词表30通过，固定岗位排序不变。业务文件仍待用户亲手修改。
- 实施补充：用户已完成业务词表修改，报告专项30通过（0.41秒）；AI核对文件并运行完整后端117项通过（9.21秒）。真实页面后续复查，不将现有测试全部通过视为通用匹配准确性保证。
- 缺点及升级条件：这是保守的有限词表，可能漏掉未列技术或没有技术名的真实经历；仍是子串任意命中，不识别学习、否定或实际职责是否一致。后续扩展需具体正反例，不把“未命中”当作无能力，也不声称事实验证完成。

## 2026-09-03 方向评分使用全栈而非宽泛AI（待用户实施）

- 问题：AI运营与AI开发仅因共同AI获得10分；同时，现有词表缺少全栈，导致单独全栈推荐无法命中全栈岗位。当前简历推荐列表确实包括全栈开发工程师，不能简单取消其10分。
- 选择：方向词表删除AI并加入全栈，保留已有Python、Vue、后端、前端、产品及10分权重。用正反例同时防止错误加分和漏匹配，修改的是依据而非追求更低分数。
- 验证：6组新增测试下旧代码3失败/33通过；进程内模拟新词表36通过，业务修改留给用户。当前数据库推荐列表的10分应由共同方向全栈解释，不再由AI解释。
- 边界：输入来自AI推荐岗位，不是已验证的工作经历；该分只能表示粗粒度方向相符。未知职业可能漏匹配，共享更具体的词仍不能证明能力或职责一致；后续按真实案例改进，不增加模型调用或扩大本轮架构。

## 2026-09-04 报告保存与无引用判断降级

- 问题：重复生成报告有成本；单条判断缺少引用会导致整份报告失败。
- 选择：保存岗位输入和完整报告快照，历史读取数据库，不重新调用模型。
- 权限：列表和详情均按当前用户过滤，越权详情与不存在记录均返回404。
- 选择：有依据或部分支持却无引用时，改为待核对并替换理由；不编造引用。
- 保留：要求遗漏、重复或未知编号、引用越界等错误仍拒绝。
- 备选：整份拒绝或额外调用模型修复；当前选择明确降级，避免该类问题触发额外调用。
- 限制：尚未保证语义判断准确；模型与提示词版本尚未记录；前端仅展示最近20份。
- 后续：数据库结构变更引入迁移管理，避免把create_all当作更新表结构的工具。

## 2026-09-05 投递记录独立建表

- 问题：投递进度属于个人，不能写到公共岗位上。
- 选择：Application关联当前用户和保存的报告，记录状态、备注与时间。
- 去重：report_id设置唯一约束；重复添加已有报告时返回原记录。
- 权限：创建前检查报告归属，查询和更新按当前用户过滤。
- 备选：直接在报告表增加状态字段；独立表便于区分分析结果和投递过程。
- 限制：按报告而非真实岗位去重；当前仅做人工跟踪，不对外自动投递。
- 后续：部署前完善数据库迁移；出现实际并发需求时补并发冲突处理与测试。

## 2026-09-05 匹配规则实施确认

- 此前方向词表“待用户实施”的方案已完成：删除AI，保留全栈，方向权重仍为10分。
- 技能别名统一、经历词表收紧及方向修正，匹配专项与固定岗位评测共36项通过。
- 修改判断依据而非降低权重；仍可能漏匹配，不把关键词命中当成真实能力证明。

## 2026-09-06 开发数据库从SQLite迁移到MySQL 8.4

- 问题：SQLite适合单机学习和快速开发，但目标岗位与部署路线要求掌握MySQL；已有用户、简历、报告和投递数据不能丢失，测试也不能污染开发数据库。
- 选择：运行环境通过`DATABASE_URL`连接MySQL 8.4，使用权限限定在`ai_job_agent`库的`job_app@localhost`账号；测试启动前覆盖为独立SQLite文件。
- 数据迁移：保留SQLite备份，按父表到子表的顺序复制ORM字段；每张表后`flush()`确保父记录可供外键检查，全部成功后统一`commit()`，异常时统一`rollback()`。
- 备选方案：继续使用SQLite；手工导出导入SQL；使用Alembic或专用ETL工具。当前数据量只有45条，使用可审查的一次性SQLAlchemy脚本更适合第一遍学习。
- 为什么选择：能复用现有ORM模型、保留主键和外键关系，同时学习环境变量、驱动、事务和数据库隔离；不为小规模一次性迁移增加额外工具复杂度。
- 验证：SQLite与MySQL六张表数量完全一致；完整测试137项通过、1项跳过；实际MySQL配置下健康接口和岗位接口返回200，中文数据精确比较正常。
- 缺点：`merge()`逐条查询和写入，数据量大时性能较差；脚本没有迁移版本、检查点和并发写入保护，正式执行期间需要停止应用写入。
- 未来升级：上线前引入Alembic管理表结构变化；数据规模增大时使用批量迁移、校验摘要、迁移日志和可恢复检查点；制定MySQL备份与恢复流程。

## 2026-09-06 Redis缓存岗位要求分析

- 问题：同一岗位描述可能在岗位匹配和报告流程中重复分析；每次都调用LLM会增加延迟、Token费用和限流风险。
- 选择：开发环境通过WSL运行Redis，后端通过可配置的`REDIS_URL`连接；使用共享Redis客户端保存JSON。以缓存版本、模型名称和岗位描述的SHA-256摘要生成键，默认TTL为1小时。
- 缓存策略：先缓存不含简历隐私的`JobRequirementResult`。命中后仍用Pydantic验证；未命中、Redis异常或缓存结构无效时回退到原模型流程，只把成功验证的模型结果写入缓存。
- 备选方案：不缓存；使用进程内字典；把结果长期保存到MySQL；直接引入LangChain缓存。当前选择Redis是因为可跨进程共享、支持TTL并符合部署目标，同时保持现有直接调用LLM的链路可控。
- 为什么不先缓存完整报告：完整报告包含个人简历资料，需要设计用户隔离、内容变更后的失效规则和更严格的数据保护；公共岗位分析风险更低，适合作为第一版最小接入点。
- 缺点：新增Redis外部依赖；故障降级后会重新产生模型费用；当前没有命中率、延迟和Token节省指标；TTL到期前Prompt逻辑变化可能短暂复用旧结果，因此键中加入`v1`用于手动升级隔离。
- 未来升级：Docker阶段由Compose提供Redis并通过环境变量使用容器服务名；收集命中率和节省成本；缓存个性化报告前加入用户范围、简历内容摘要、Prompt版本和明确失效策略。

## 2026-09-06 使用Docker Compose编排本地后端依赖

- 问题：后端、MySQL和Redis分别手工启动时，环境难以复现，端口、连接地址和启动顺序也容易不一致。
- 选择：使用根目录`compose.yaml`编排三个服务；后端从`backend/Dockerfile`构建，通过Compose服务名访问MySQL和Redis，并等待两者健康后再启动。
- 数据与缓存：MySQL使用命名卷`mysql_data`保存业务数据；Redis当前只存可重新生成的缓存，不配置持久化卷。
- 配置与安全：真实开发值保存在被Git忽略的`.env`中；仓库提交`.env.example`作为字段清单，不提交密码和`SECRET_KEY`。
- 端口选择：主机使用8000访问后端；MySQL映射3307以避开宿主机3306；Redis映射6380以避开WSL中的6379。容器之间仍使用各自标准端口。
- 备选方案：继续连接宿主机服务；为每个容器单独执行`docker run`；现在就引入Kubernetes。Compose更适合当前单机开发和第一版部署训练，Kubernetes会增加当前不需要的复杂度。
- 验证：后端镜像可以独立启动；Compose内数据库查询返回`ai_job_agent`，Redis连接返回`True`；停止并重新启动后，三个服务均通过健康检查。
- 缺点：Compose数据库是独立的新实例，尚未导入宿主机MySQL的45条业务数据；上传文件没有持久卷；Embedding模型缓存未打包或挂载，相关语义/RAG功能在容器内仍需专项验收；前端、Nginx和HTTPS尚未进入编排。
- 未来升级：先补数据库初始化或受控迁移与备份恢复，再处理上传持久化和Embedding资源；部署阶段加入Vue构建、Nginx反向代理、HTTPS、日志与监控。

## 2026-09-06 Vue生产环境使用Nginx同源代理与多阶段镜像

- 问题：开发环境由Vite提供页面、前端直接请求8000端口；生产容器需要提供编译后的静态文件、支持Vue History路由，并让浏览器通过稳定入口访问后端。
- 选择：Axios统一使用`/api`相对地址。开发时由Vite代理到`127.0.0.1:8000`并删除前缀；生产时由Nginx提供`dist`、用`try_files`回退到`index.html`，并把`/api/`代理到Compose服务`backend:8000/`。前端镜像使用Node构建、Nginx运行的多阶段Dockerfile。
- 备选方案：前端在生产环境直接请求公开的后端8000端口；在运行时继续使用Vite开发服务器；把Node构建工具和Nginx放在同一个最终运行镜像。当前方案入口统一、运行镜像更小，也避免浏览器生产请求跨域。
- 为什么选择：同一套`/api`调用代码可用于开发和生产，只由各自代理决定后端位置；Nginx适合提供静态文件和反向代理；多阶段构建不会把Node依赖与源码留在最终运行层。
- 验证：Vite的`5173/api/health`与Nginx的`8080/api/health`均返回`ok`；直接访问`8080/jobs`由Vue接管并按认证状态跳转，登录后成功读取3个岗位。
- 缺点：开发与生产使用两套代理配置，前缀删除规则必须保持一致；Compose当前仍把后端8000端口映射到主机；Nginx只有HTTP配置，尚无HTTPS、安全响应头、压缩策略和访问日志方案。
- 未来升级：部署服务器时只对外开放Nginx的80/443端口，收敛后端和数据库端口；增加域名、HTTPS、日志轮转、上传文件持久化和各AI功能的容器内验收。

## 2026-09-06 上传简历使用独立命名卷并保持非root运行

- 问题：数据库使用命名卷后记录可以保留，但简历文件仍写在后端容器的`/app/uploads`临时层；容器重建会造成记录存在而文件丢失。首次挂载的新卷还由`root`拥有，`appuser`无法写入。
- 选择：为后端挂载独立命名卷`upload_data:/app/uploads`；镜像构建时创建该目录并设为`appuser:appuser`，应用继续使用非root用户运行。已经创建的卷只执行一次限定路径的所有权修复。
- 备选方案：把宿主机目录直接绑定到容器；把文件存入MySQL；让后端长期以root运行。命名卷更符合当前Compose训练，数据库不适合直接保存这些文件，长期root运行会扩大安全风险。
- 验证：运行用户为`appuser`，修复前目录属于`root root`并出现`Permission denied`；修复后目录属于`appuser appuser`，测试文件在后端镜像重建和容器重建后仍返回`volume-ok`。
- 缺点：命名卷不等于备份；当前文件与数据库写入不是同一个事务，仍可能出现文件写入成功但数据库提交失败等不一致；尚未完成真实PDF的容器内上传、下载和删除验收。
- 未来升级：增加定期备份与恢复演练；用无敏感测试PDF覆盖上传、下载、删除和越权测试；部署规模或多实例需求出现后评估对象存储，并为文件与数据库失败设计补偿清理。
