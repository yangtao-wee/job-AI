# AI Job Agent 学习进度

> 北极星目标：用自己开发并真实使用的 AI Job Agent 获得面试和工作机会，并能向面试官讲清核心代码、数据路线、设计选择、Bug 与结果。
>
> 当前五阶段主路线、完成门禁和代码学习分类见 [`V2_ROADMAP.md`](./V2_ROADMAP.md)。新会话继续项目前必须先读取该文件。

## 2026-08-27

- 完成功能：Git基线版本、岗位技能匹配、JD关键词匹配、岗位JD结构化Schema、Prompt、Mock/真实模型分流、受JWT保护的岗位分析接口。
- 用户亲手完成：匹配函数、结构化Schema、岗位AI Service、岗位分析Router及Debug修复。
- AI生成内容：本学习记录与技术决策记录。
- 🟡 正在学习：Git、FastAPI路由、Pydantic Structured Output、Mock Mode、HTTP状态码。
- 🔴 未掌握：RAG、Agent、Tool Calling、MySQL、Redis、自动化测试、Docker部署。
- Bug：`Job.id == job.id`在变量赋值前读取`job`，导致接口返回500。
- 修复：改为使用路径参数`Job.id == job_id`，验收结果为200、401、404。
- 项目证据：首次Commit `ff96549`；岗位分析接口三分支测试通过。
- 下一步：从Vue通过Axios调用岗位分析接口并展示结构化结果。

## 2026-08-28

- 完成功能：Vue通过Axios调用岗位分析接口，JobCard使用Props和Emit完成父子通信，并展示岗位职责、必备技能、经验、学历和加分项。
- 用户亲手完成：`analyzeJob` API函数、`handleAnalyze`页面函数、分析按钮、加载状态和五类结果模板。
- AI生成内容：更新本学习记录；执行编译、接口和浏览器运行时验收。
- 🟡 正在学习：Vue Props/Emit、异步状态、前后端字段映射、运行时Debug。
- Bug：`requirements`单复数不一致、`responsibilities`拼写错误、字段中文标签映射错误。
- 修复：统一字段名称与标签；生产编译通过；浏览器真实点击后五类结果正确显示，控制台无错误。
- 项目证据：岗位分析前端闭环、200/401/404接口测试、真实页面运行结果。
- 完成功能：把AI提取的岗位必备技能加入匹配结果，页面展示总分、规则技能、关键词和AI必备技能三个维度。
- 用户亲手完成：必备技能评分纯函数、响应Schema、后端评分组合、Vue字段映射和结果展示。
- 验收结果：后端正常、越权和岗位不存在三种请求通过；前端生产构建通过；浏览器显示总分41/60和三个分项分数。
- Bug：在返回对象内部错误创建变量导致NameError；尾部逗号把AI结果变成元组导致AttributeError。
- 修复：先计算各分项结果再构造响应；删除多余逗号并重新执行接口测试。
- 项目证据：形成可解释的多维评分界面和完整Debug案例。
- 🟡 正在学习：纯函数、分项权重、前后端评分字段映射、自动化测试。
- 完成测试：为必备技能评分补充部分匹配、空要求、完全不匹配三种用例，结果为 `3 passed`。
- 工程清理：将Pydantic旧版 `class Config` 改为 `ConfigDict`，测试警告消失；将pytest缓存加入`.gitignore`。
- 用户亲手完成：测试文件、Pydantic配置升级和测试缓存忽略规则。
- 项目证据：评分纯函数具备第一组自动化回归测试，覆盖正常、边界和不匹配情况。
- 下一步：修正技能分数重复计算问题，建立不重复计分的100分评分框架。
- 完成功能：合并数据库岗位技能和AI提取的必备技能，统一格式并去重，只计算一次正式技能分。
- 用户亲手完成：`merge_job_skills`、评分函数列表输入改造、路由计算顺序调整和回归测试。
- 验收结果：4个单元测试通过；真实匹配接口返回200和26/45，其中技能23分、关键词3分。
- Bug：新增测试误写为`text_`，pytest只收集3项，导致测试实际没有执行。
- 修复：改为`test_`前缀，确认`collected 4 items`和`4 passed`。
- 项目证据：形成“多来源技能合并—去重—单次计分”的可解释评分链路。
- 下一步：为简历增加结构化经历数据，为30分职责/经历匹配做准备。
- 完成功能：为简历分析增加结构化 `work_experience` 字段，并通过迁移、ORM、Schema、Mock和保存Service接通数据库。
- 用户亲手完成：SQLite迁移脚本、模型字段、结构化输出字段、Prompt要求、Mock数据和保存赋值。
- 验收结果：真实数据库字段存在；临时内存数据库可以保存并重新读取工作经历；原有4个匹配测试继续通过。
- Bug：迁移文件最初放在 `app/migrations` 且文件名误写为 `worl`，导致模块找不到；SQL字符串还缺少空格。
- 修复：移动到 `backend/migrations`、统一文件名为 `work`，并修正完整的 `ALTER TABLE` SQL。
- 项目证据：掌握已有SQLite表的向后兼容字段迁移，以及Schema—ORM—Service持久化链路。
- 下一步：实现工作经历与岗位职责的30分匹配结果Schema和第一版纯函数。
- 完成功能：新增30分经历/职责规则匹配，返回匹配职责、简历证据和缺失职责，并接入岗位匹配接口与Vue岗位卡片。
- 用户亲手完成：`ExpMatch`结果Schema、证据查找函数、经历评分函数、Router组合、Vue数据传递与证据展示。
- 验收结果：6个单元测试通过；前端生产编译通过；浏览器真实页面显示总分56/75、经历30/30、岗位职责和简历证据；控制台无错误。
- Bug：评分分母误用单条职责字符串长度，导致15分错误计算为3分；Vue列表`:key`误写成复数字段。
- 修复：分母改为职责列表数量；前端统一使用后端正式字段`responsibility`。
- 学习发现：旧简历分析的经历字段为空，需要重新分析；Mock测试数据必须分别覆盖匹配和不匹配分支。
- 项目证据：形成“规则评分—证据说明—缺口展示”的可解释岗位匹配闭环。
- 🟡 正在学习：业务规则测试、前后端嵌套字段映射、测试数据设计、运行时验收。
- 下一步：补齐剩余25分基础规则维度，并逐步形成100分岗位匹配框架。
- 完成功能：增加10分岗位方向匹配，将岗位名称与AI推荐岗位方向进行规则比对，并把分数和原因展示到Vue岗位卡片。
- 用户亲手完成：`RoleMatch`响应结构、`score_role`纯函数、Router计分组合、2个单元测试和Vue字段展示。
- 验收结果：8个后端测试通过；前端生产构建通过；浏览器真实页面显示66/85、岗位方向10/10和“共同方向:Python”；控制台无报错。
- 项目证据：岗位推荐现在可以解释技能、关键词、经历和岗位方向四个维度。
- 🟡 正在学习：评分权重设计、前后端字段契约、真实页面运行时验收。
- 下一步：增加求职城市和最低期望薪资，为剩余15分偏好匹配做准备。
- 完成功能：增加城市5分和薪资10分的求职偏好匹配，Vue支持输入条件并展示分数与中文原因，第一版规则评分达到100分。
- 用户亲手完成：请求与响应Schema、薪资数字提取、偏好评分函数、Router组合、3个单元测试、Vue表单、Axios传参和JobCard展示。
- 验收结果：11个后端测试通过；前端生产构建通过；真实页面显示81/100、求职偏好15/15、城市和薪资均符合。
- Bug：薪资最初使用`>`判断，岗位最高薪资刚好等于用户最低要求时会被误判。
- 修复：改用`>=`并增加相等边界测试。
- 安全问题：登录成功日志输出整个响应，导致JWT显示在浏览器控制台。
- 修复：删除敏感登录响应日志；重新登录后控制台不再显示Token。
- 项目证据：完成技能、关键词、经历、岗位方向和求职偏好五维可解释规则评分，以及正常、异常、边界测试。
- 🟡 正在学习：评分规则设计、边界测试、Vue表单到FastAPI的数据链路、安全日志。
- 下一步：建立固定岗位匹配评测集，记录正确案例和错误案例，再进入Embedding语义匹配。
- 完成功能：建立第一版固定岗位评测集，包含Python、Vue和销售3个岗位，并验证预期排名与基线分数。
- 用户亲手完成：`match_cases.json`评测数据和`test_match_eval.py`整体排名测试。
- 验收结果：Python岗位95分排第1、Vue岗位45分排第2、销售岗位15分排第3；全部12个测试通过。
- Bug：排序和断言最初缩进在`for`循环内部，导致只处理第一个岗位就开始验收。
- 修复：将排序和断言移出循环，完整处理3个岗位后再比较结果。
- 项目证据：拥有规则匹配基线数据，后续可以量化比较Embedding加入前后的推荐变化。
- 🟡 正在学习：评测集、基线、回归测试、从pytest差异定位缩进问题。
- 下一步：设计语义匹配输入输出，先理解Embedding解决规则匹配的哪个真实缺陷。
- 完成功能：新增本地中文Embedding语义服务，使用`BAAI/bge-small-zh-v1.5`把两段文字转换为向量并计算相似度。
- 用户亲手完成：模型延迟加载与缓存、`calc_sim`相似度函数、相近文本高于无关文本的自动测试。
- 验收结果：接口开发与REST API相似度为0.590，接口开发与门店销售相似度为0.403，相对顺序正确；语义测试1项通过。
- Bug：评测JSON末尾加入`//`注释后触发`JSONDecodeError: Extra data`；删除非法注释后原排名测试恢复通过。
- 项目证据：建立第二层语义匹配的本地模型能力与第一条Embedding回归测试，不产生外部API Token费用。
- 🟡 正在学习：Embedding、文本向量、模型缓存、相对相似度评测。
- 下一步：为岗位匹配响应增加语义结果字段，先作为参考值展示，不立即修改100分规则总分。
- 完成功能：将本地中文语义模型接入`/jobs/match`真实接口，并通过Vue岗位卡片展示语义参考值、模型名称和中文说明。
- 用户亲手完成：`SemMatch`响应Schema、Router语义输入组合、Service调用、响应字段、父子组件传值和页面展示。
- 验收结果：真实Python岗位页面显示语义参考值0.737；规则总分仍为81/100；后端13项测试通过；前端生产构建通过；浏览器控制台无错误。
- Bug：简历和岗位文字最初使用`''.join()`直接粘连，虽然不报错但会降低英文技能词的分隔质量；改为`' '.join()`。
- 性能证据：真实页面首次模型请求约25秒；完整测试因加载本地模型耗时147秒，需要后续区分快速测试和AI慢测试。
- 项目证据：完成“规则匹配层 + Embedding语义层 + 可解释页面展示”的端到端闭环，并保留100分规则基线用于后续对比。
- 🟡 正在学习：语义输入设计、模型延迟加载、Vue嵌套响应映射、AI慢测试。
- 下一步：记录项目直接依赖并为语义测试增加慢测试标记，保证项目可复现且日常测试高效。
- 完成功能：新增运行依赖与开发依赖清单，并用pytest标记把本地Embedding模型测试划分为慢测试。
- 用户亲手完成：`requirements.txt`、`requirements-dev.txt`、`pytest.ini`和`@pytest.mark.slow`测试标记。
- 验收结果：`pip check`无依赖冲突；pip dry-run确认全部固定版本可识别；快速测试12项通过、1项主动跳过，耗时6.41秒；慢测试收集准确命中1项。
- 性能改进：日常反馈从完整测试147秒降低到快速测试6.41秒，发布前仍可运行全部13项测试。
- 项目证据：具备可复现Python环境、运行/开发依赖分层和AI慢测试分层。
- 🟡 正在学习：依赖锁定、测试选择、快速反馈与完整验收的区别。
- 下一步：把Embedding加入固定岗位评测集，比较规则基线与语义层的推荐排序变化。
- 完成功能：将本地Embedding加入固定3岗位评测集，并验证Python、Vue、销售岗位的语义推荐顺序。
- 用户亲手完成：语义排名慢测试，以及本地模型只读参数`local_files_only=True`。
- 验收结果：语义分依次为0.884、0.646、0.335，岗位顺序为1、2、3；2项语义慢测试通过；12项快速测试继续通过。
- Bug：模型文件已经下载，但加载器仍会访问Hugging Face检查更新，断网时产生多次重试并拖慢测试。
- 修复：模型加载增加`local_files_only=True`，本地权重加载约0.15秒，语义测试总耗时约6.87秒。
- 项目证据：拥有“规则基线排名 + Embedding语义排名”的可重复对比测试，并证明当前3个代表岗位的推荐顺序符合人工预期。
- 🟡 正在学习：语义排序评测、离线模型加载、AI快慢测试分层。
- 下一步：在现有岗位分析Structured Output基础上，设计第三层LLM推荐解释结果，不让大模型直接篡改规则总分。
- 完成功能：新增第三层LLM岗位匹配解释，规则层负责100分评分，Embedding层提供语义参考，LLM层只根据证据生成总结、理由、缺口和行动建议。
- 用户亲手完成：`MatchExplain`结构、Prompt、Mock解释、真实LLM结构化输出调用、Router组合、Vue展示和2个自动化测试。
- 验收结果：真实页面成功显示AI岗位建议；14项快速测试通过、2项慢测试主动跳过；前端生产构建通过。
- Bug：父组件最初使用`ai_note`传递属性，子组件接收的是`aiNote`，导致AI建议不显示；修正Vue Props名称后恢复。
- 安全设计：Prompt明确要求不得修改规则分、不得编造经历，并把岗位资料标记为待分析数据，降低Prompt Injection风险。
- 项目证据：形成“规则评分 + Embedding语义参考 + LLM结构化解释”的三层可解释岗位推荐架构，以及Prompt和Mock边界回归测试。
- 🟡 正在学习：Structured Output、Prompt约束、Mock测试、LLM解释与规则评分的职责边界。
- 下一步：为真实LLM调用增加超时、重试和异常降级，避免外部模型故障拖垮岗位匹配接口。
- 完成功能：为共享OpenAI客户端增加30秒超时和最多2次自动重试；岗位解释失败时记录异常并返回规则生成的降级解释。
- 用户亲手完成：超时与重试配置、客户端参数、`try/except`降级代码和monkeypatch离线故障测试。
- 验收结果：临时环境值7能够正确覆盖重试默认值；故障测试3项通过；全部快速测试15项通过、2项慢测试主动跳过；FastAPI接口成功加载。
- Bug：配置字段最初误写为`llm_max_retrier`，因为Python引用也同步写错且默认值为2，导致程序能运行但环境变量不能覆盖；使用不同临时值7发现并修复。
- Bug：降级测试预期值将`Docker`误写为`Doker`，根据pytest最后的实际值与预期值对比定位并修复。
- 安全问题：尝试用PowerShell空字符串覆盖密钥时，空值实际删除了临时变量，程序重新读取`.env`并发出真实请求；以后外部故障测试统一使用monkeypatch，旧密钥需要撤销更换。
- 项目证据：具备外部LLM的Timeout、Retry、Fallback、异常日志和完全离线故障回归测试。
- 🟡 正在学习：外部服务异常边界、默认值掩盖配置错误、monkeypatch和安全测试隔离。
- 下一步：为简历分析和岗位分析两个LLM入口统一增加相同的降级策略，避免只保护岗位解释接口。

- 完成功能：AI简历分析增加连接失败、请求失败和空结果降级。
- 用户亲手完成：make_fail、ai_ok状态、异常日志和3项故障测试。
- 验收结果：3项故障测试通过；全部快速测试18项通过，2项慢测试主动跳过。
- 数据保护：ai_ok=False的降级结果不会写入数据库。
- 🟡 正在学习：外部AI异常处理、状态标志、回归测试。

- 完成功能：岗位LLM解释增加Token用量和单次费用统计。
- 用户亲手完成：TokenUse、read_use、calc_fee、费用日志和费用测试。
- 验收结果：正常费用3.0、零用量0.0，7项岗位AI服务测试通过。
- Bug：日志增加费用占位符后，最初遗漏fee参数；补齐后验证通过。
- 项目证据：具备LLM输入、输出、总Token和单次费用监控能力。
- 🟡 正在学习：Token计费、成本日志和费用边界测试。
- 下一步：让AI简历分析也记录Token和费用，统一两个LLM入口。

## 统一LLM成本统计

- 完成功能：岗位解释和简历分析共同复用llm_cost.py。
- 数据路线：LLM响应 → usage → TokenUse → 费用计算 → 日志。
- 验收结果：22 passed，2个慢测试按计划跳过。
- 正在学习：重构、单一职责、回归测试。
- 下一步：进入RAG，先学习文本切分。

## 2026-08-30 RAG核心服务第一版

- 完成功能：实现文本切分、Embedding向量生成、相似度检索、上下文拼接、安全Prompt、结构化RAG回答和异常降级。
- 用户亲手完成：`split_text`、`make_vecs`、`search`、`make_ctx`、`make_prompt`、`answer_question`及正常、异常、边界测试。
- 数据路线：问题与资料 → Embedding → 相似度检索 → 上下文 → Prompt → LLM → `RagAnswer` → 结果验证。
- 异常保护：无资料、客户端连接失败、LLM请求失败和结构化结果为空时统一返回安全降级结果。
- 验收结果：RAG测试12项快速测试通过、2项慢测试按计划跳过；后端完整回归测试38项全部通过。
- Bug：中英文冒号导致断言失败；`ReferenceError`语义不合适；`self`误写；空结果测试最初因假客户端对象层级错误而测试了错误分支。
- 修复：统一预期字符串；使用`RuntimeError`；修正实例参数；用`caplog`确认空结果测试命中正确日志分支。
- 项目证据：形成覆盖正常、异常和边界情况的RAG Service测试集，并具备Token用量和费用日志。
- 🟡 正在学习：RAG完整数据路线、测试替身、分支证明、结构化输出验证。
- 下一步：为RAG问答增加FastAPI接口，接通HTTP请求与Service。

## 2026-08-30 RAG问答API第一版

- 完成功能：新增`RagAsk`请求Schema、`POST /rag/ask`路由、JWT认证、输入限制和`RagAnswer`响应验证。
- 用户亲手完成：请求Schema、RAG Router、主应用路由注册，以及200、401、422三类API测试。
- 数据路线：JSON请求 → Pydantic验证 → JWT认证 → Router → RAG Service → `RagAnswer` → JSON响应。
- 验收结果：RAG API测试3项通过；后端完整回归测试41项全部通过。
- Bug：最初使用`app.routes`逐项读取`path`，遇到FastAPI内部`_IncludedRouter`后产生`AttributeError`。
- 修复：改用`app.openapi()['paths']`检查最终接口路径，确认`POST /rag/ask`注册成功。
- 安全设计：匿名请求返回401；空问题和空资料返回422；无效请求不会进入Embedding和LLM流程。
- 项目证据：具备RAG接口的正常、认证失败和输入边界自动化测试。
- 🟡 正在学习：Router与Service分层、依赖覆盖、HTTP接口安全边界。
- 下一步：由Vue调用RAG接口，完成前后端问答闭环。

## 2026-08-30 RAG前后端问答闭环

- 完成功能：新增Vue RAG请求封装、受保护的`/rag`页面路由，以及提问、加载、成功和失败状态展示。
- 用户亲手完成：`askRag`、`RagView.vue`、RAG前端路由和页面状态处理。
- 数据路线：用户输入 → Vue `ask` → Axios `POST /rag/ask` → JWT认证 → RAG Service → `RagAnswer` → Vue展示。
- 验收结果：前端生产构建成功，88个模块完成转换；浏览器成功显示模拟回答、资料是否足够和“使用Docker部署服务”资料来源，知识问答导航可正常进入页面。
- 成本保护：当前联调使用Mock模式，不产生真实LLM调用费用；RAG请求单独使用30秒超时。
- 项目证据：形成从浏览器、Axios、FastAPI到RAG Service再返回页面的完整联调截图。
- 🟡 正在学习：Vue异步状态、前后端联调、前端路由保护与后端JWT的职责区别。
- 下一步：提交前端RAG功能，再把客户端固定资料升级为服务端管理的真实知识库。

## 2026-08-31 RAG服务端知识库与相关性阈值

- 完成功能：资料从Vue固定参数迁移到后端`job_guide.txt`，由`load_parts()`统一读取；RAG接口只接收问题，并通过`extra='forbid'`拒绝客户端伪造资料字段。
- 用户亲手完成：`kb_service.py`、后端知识文件、Router接入、严格请求Schema、前端请求收缩、相关性阈值和正反测试。
- 数据路线：Vue问题 → JWT认证 → 后端知识文件 → Embedding → Top-K排序 → `min_score=0.5`过滤 → 回答或安全拒答。
- 验收结果：RAG组合测试17项通过、2项慢测试跳过；后端完整回归43项通过；前端生产构建88个模块成功；浏览器验证Docker问题正常回答、无关问题`as`安全拒答。
- Bug：无意义问题最初只要知识库非空就会返回`enough=True`；相同别名`rag`在API测试中指向Router，导致Service阈值测试放错文件。
- 修复：实测无关问题最高分0.3965、Docker问题最高分0.7801，第一版采用0.5阈值；把`test_ctx_low`移动到Service测试文件。
- 项目证据：具备服务端可信资料边界、严格API契约、相关性阈值、无关问题拒答截图和43项后端回归结果。
- 🟡 正在学习：RAG检索阈值、误检与漏检、服务端知识库边界、跨层回归测试。
- 下一步：保留每条检索来源及分数，不再把多段上下文合并成一个来源。

## 2026-08-31 RAG检索来源拆分

- 完成功能：检索结果同时保留分数和独立资料来源，不再把多段上下文作为一个来源返回。
- 数据路线：search → pick_rows → sources/ctx → LLM → 后端覆盖可信sources。
- 测试改进：让假模型返回编造来源，证明后端会使用真实检索来源覆盖。
- 验收结果：后端完整回归44项通过，来源覆盖专项测试通过。
- 🟡 正在学习：RAG来源可信性、测试假阳性、检索结果与模型上下文分离。
- 下一步：让接口返回每条检索来源对应的相似度分数。

## 2026-08-31 RAG来源与相似度展示

- 完成功能：新增`RagSrc`，每条来源同时返回资料文字和相似度分数。
- 数据路线：检索元组 → `RagSrc` → `RagAnswer` → FastAPI JSON → Vue展示。
- 验收结果：Service快速测试14项、API测试4项、后端完整回归44项通过；Vue构建成功。
- 浏览器结果：Docker问题检索分数为0.77、0.56和0.55，页面能够分别展示。
- 项目证据：形成RAG检索来源可解释性截图，并具备后续阈值和Top-K调优数据。
- 🟡 正在学习：嵌套Schema、API数据契约、检索质量分析。
- 下一步：根据实际分数减少弱相关资料进入Prompt。

## 2026-08-31 RAG相关性阈值调优

- 问题：0.5阈值会让Docker问题同时召回FastAPI和Vue弱相关资料。
- 评测结果：三个正确资料最高分为0.7746、0.6969和0.7041；弱相关资料最高分为0.5616；无关天气问题最高分为0.2503。
- 调整方案：新增统一常量`MIN_SCORE=0.6`，`pick_rows`和`make_ctx`共同使用。
- 边界测试：0.6保留，0.59过滤。
- 浏览器结果：Docker问题只保留0.77的Docker资料，不再返回0.56和0.55的弱相关资料。
- 验收结果：RAG快速测试14项通过，后端完整回归44项通过。
- 🟡 正在学习：检索精确率、召回率、评测集和阈值调优。
- 下一步：进入Agent基础，先区分普通LLM调用、RAG和Agent。

## 2026-08-31 Agent Service基础闭环

- 完成功能：新增知识库工具定义、工具白名单分发、工具调用结果转换和一轮Agent执行流程。
- 数据路线：用户目标 → Chat Completions API → tool_calls → 白名单工具 → 知识库检索 → tool消息 → 最终回答。
- 测试覆盖：工具正常执行、未知工具、请求结构、工具分支、直接回答、Mock模式、真实模式入口和缺少模型配置。
- 验收结果：Agent专项测试10项通过，后端完整回归54项通过。
- 成本控制：单元测试全部使用假客户端；模型直接回答时不会发送第二次请求。
- 🟡 正在学习：Agent编排、Tool Calling、调用关联、工具安全边界。
- 下一步：为Agent增加受JWT保护的FastAPI接口。

## 2026-08-31 Agent API与认证

- 完成功能：新增`POST /agent/ask`，通过JWT认证后调用Agent Service。
- 数据路线：HTTP请求 → JWT认证 → AgentAsk → ask_agent → AgentAnswer → JSON响应。
- 安全边界：匿名请求返回401，空目标和额外字段返回422，不进入收费模型。
- 测试覆盖：未登录、正常回答、空白目标和额外字段。
- 验收结果：Agent API专项测试4项、后端完整回归58项通过。
- 🟡 正在学习：Agent API分层、模型成本保护、接口隔离测试。
- 下一步：新增Vue Agent页面并调用`/agent/ask`。

## 2026-08-31 Vue Agent页面联调

- 完成功能：新增Agent API函数、`/agent`页面、导航入口和登录路由保护。
- 数据路线：Vue目标输入 → Axios携带JWT → `/agent/ask` → Agent Service → AgentAnswer → 页面展示。
- 成本控制：请求期间使用`loading`禁用按钮，避免重复提交和重复模型费用。
- 浏览器结果：页面成功显示后端返回的模拟Agent回答。
- 验收结果：Vite生产构建成功，共转换89个模块。
- 项目证据：形成Agent前后端Mock闭环截图，可证明接口、认证和页面链路已经接通。
- 🟡 正在学习：Agent前后端联调、异步状态、模型成本保护。
- 下一步：受控开启真实模型，验证`find_kb`工具选择和执行。

## 2026-08-31 真实模型Tool Calling验收

- 完成功能：Agent迁移到智谱兼容的Chat Completions Tool Calling。
- 真实验证：模型成功选择`find_kb`，本地Embedding完成检索并生成知识库回答。
- 故障案例：`glm-4.7-flash`返回1305服务拥堵，临时切换免费备用模型后调用成功。
- 验收结果：Agent专项测试10项、后端完整回归58项全部通过。
- 项目证据：形成真实LLM → Tool Calling → RAG检索 → 最终回答的完整执行记录。
- 🟡 正在学习：模型重试、备用模型和生产可用性。
- 下一步：为真实模型调用增加受控重试和免费备用模型。

## 2026-09-01 Agent主备模型切换

- 完成功能：为Agent模型调用增加主模型繁忙降级；仅当智谱返回`1305`且已配置备用模型时切换。
- 数据路线：Agent消息 → 主模型 → `RateLimitError` → 错误码和备用配置判断 → 备用模型 → Tool Calling → 最终回答。
- 用户亲手完成：拆分`call_model`、实现`ask_model`异常分支，并补充主备切换、非繁忙错误、未配置备用模型三个测试场景。
- 安全边界：非`1305`错误原样抛出；没有备用模型时原样抛出，避免隐藏认证、参数或程序错误。
- 验收结果：Agent Service测试13项、后端完整回归61项通过；备用模型真实调用`find_kb`并返回知识库中的Docker答案。
- 项目证据：形成Mock异常分支测试、完整回归结果和真实备用模型Tool Calling记录。
- 🟡 正在学习：异常分类、主备模型容错、自动化测试与真实冒烟测试的区别。
- 下一步：设计统一LLM适配层，再逐个迁移仍使用旧调用方式的Service。

## 2026-09-01 统一LLM结构化调用与岗位服务迁移

- 完成功能：在`llm_service.py`中统一客户端创建、JSON消息构造、空结果检查和Pydantic结构验证；`ai_job_service.py`不再直接使用旧的`responses.parse`。
- 数据路线：岗位描述 → 岗位Prompt → `call_structured` → 智谱Chat Completions → JSON正文 → `JobRequirementResult`验证 → 岗位业务结果。
- 用户亲手完成：`build_json_messages`、`call_structured`、岗位服务迁移，以及统一调用层和岗位服务的Mock测试。
- AI处理内容：仅机械整理缩进、空格、换行和学习记录，没有改变核心执行逻辑。
- Bug与修复：`model_json_schema`最初遗漏括号，导致方法对象无法JSON序列化；测试函数名曾拼写错误导致无法选中；无效JSON测试曾误写成集合。均通过错误信息定位并修复。
- 测试覆盖：消息包含Schema、正常JSON解析、空内容拒绝、缺少必填字段拒绝、岗位服务四个参数连接。
- 真实验证：智谱成功把岗位文字解析为职责、必备技能、经验、学历和加分项。
- 验收结果：专项测试5项通过，后端完整回归66项通过，相关文件语法检查通过。
- 项目进度：统一结构化调用基础已完成；旧结构化业务服务迁移完成1/4，阶段1尚未完成。
- 掌握状态：🟢统一客户端价值、JSON外形与Schema验证、Mock测试目的；🟡Agent完整复述、测试具体写法；🔴其余业务服务独立迁移。
- 下一步：迁移`ai_resume_service.py`，继续复用相同统一调用层，不新增第二套实现。

## 2026-09-01 简历服务迁移与结构化调用容错

- 完成功能：`ai_resume_service.py`从旧的`responses.parse`迁移到统一`call_structured`；统一调用层在主模型返回`1305`繁忙错误时切换一次备用模型。
- 数据路线：简历文本 → 简历Prompt → 统一结构化调用 → 主模型/备用模型 → JSON正文 → `ResumeAIAnalysis`验证 → 后端覆盖可信`resume_id` → Token统计 → 业务结果。
- 用户亲手完成：简历服务调用迁移、统一主备模型分支、Token新旧字段兼容，以及真实冒烟测试。
- AI处理内容：机械格式整理、测试样板和学习记录；核心数据路线和异常策略由用户理解后亲手实现。
- Bug与修复：真实模型已返回结果，但`CompletionUsage`使用`prompt_tokens/completion_tokens`，旧成本代码读取`input_tokens/output_tokens`而抛出`AttributeError`；通过`getattr`兼容两套字段，并新增回归测试。
- 安全边界：模型生成的`resume_id`不作为可信数据，最终强制使用后端请求中的简历编号；模型失败时返回`ai_ok=False`的安全结果，不保存伪造分析。
- 真实验证：主模型`glm-4.7-flash`繁忙后自动切换`glm-4-flash-250414`，成功返回技能、优势、改进建议和推荐岗位，且没有编造公司、学历或工作年限。
- 验收结果：LLM统一层测试7项、简历服务测试4项、后端完整回归71项通过，真实结构化简历分析`ai_ok=True`。
- 项目进度：旧结构化业务服务迁移完成2/4；岗位和简历已完成，匹配解释与RAG仍待迁移。
- 掌握状态：🟢统一适配层价值、模型与后端的可信数据边界、Mock与真实冒烟测试区别；🟡主备容错和第三方响应字段兼容；🔴独立迁移剩余业务服务。
- 下一步：迁移`ai_match_service.py`，复用统一结构化调用、主备策略和Token兼容逻辑。

## 2026-09-02 岗位匹配解释迁移与可信字段保护

- 完成功能：`ai_match_service.py`从旧的`responses.parse`迁移到统一`call_structured`，继承主备模型切换、结构验证和Token字段兼容能力。
- 数据路线：规则分/语义分/证据/缺口 → 匹配Prompt → 统一结构化调用 → 模型总结和行动建议 → 后端覆盖可信`reasons/gaps` → Token统计 → `MatchExplain`。
- 用户亲手完成：统一调用迁移、正常成功路线返回、Prompt求职者视角约束、可信字段覆盖和故意让假模型编造的回归测试。
- 真实评测：第一次输出错误地给招聘方提出面试评估建议；增加求职者视角约束后行动建议改善，但模型仍扩大Docker缺口，因此进一步由后端强制覆盖匹配证据和能力缺口。
- 安全边界：规则分、匹配证据和能力缺口由后端负责；模型只负责总结与行动建议，不能成为事实来源。
- 测试改进：发现假模型最初返回值与真实输入相同，即使删除覆盖代码也可能通过；改为故意返回“模型编造理由/缺口”，避免测试假阳性。
- 验收结果：岗位匹配专项测试9项、后端完整回归72项通过；真实模型输出`reasons=['Python','FastAPI']`、`gaps=['Docker']`，行动建议面向求职者。
- 项目进度：旧结构化业务服务迁移完成3/4；岗位、简历、匹配解释已完成，仅RAG待迁移。
- 掌握状态：🟢模型负责语言、后端负责事实；🟢Mock测试与真实冒烟测试的分工；🟡独立设计能发现假阳性的测试数据；🔴独立完成RAG迁移。
- 下一步：迁移`rag_service.py`到统一结构化调用，保持检索来源、相似度和资料是否足够由后端控制。

## 2026-09-02 RAG统一调用迁移与阶段1验收

- 完成功能：`rag_service.py`从旧的`responses.parse`迁移到统一`call_structured`，继承主备模型切换、空内容检查、Pydantic验证和Token字段兼容。
- 数据路线：用户问题 → 本地Embedding → 相似度检索 → 0.6阈值过滤 → 可信上下文 → 统一结构化模型调用 → `RagAnswer`验证 → 后端覆盖可信`sources` → 返回结果。
- 用户亲手完成：RAG业务调用迁移，正常、请求失败和空内容三条测试路线迁移，以及真实RAG冒烟测试。
- 测试改进：删除只支持`responses.parse`的旧假客户端，改为直接替换`call_structured`；修复`call_stuctured`拼写错误和日志断言文字差异。
- 安全边界：检索资料与相似度分数由后端向量检索负责；模型只生成`answer`并判断资料是否足够，不能生成可信来源。
- 真实验证：Docker资料相似度为0.7819，高于0.6阈值；FastAPI无关资料被过滤；模型基于Docker资料正确回答，最终来源由后端覆盖。
- 验收结果：RAG专项测试16项、后端完整回归72项通过；代码库已无旧`responses.parse/output_parsed`调用。
- 阶段1结果：Agent真实Tool Calling、主备模型、统一结构化适配层、岗位/简历/匹配/RAG四个业务服务迁移、Mock测试和真实冒烟测试均已完成。
- 项目进度：阶段1“LLM基础”达到完成门禁，下一阶段进入手动JD产品，不依赖Chrome插件也能帮助真实投递。
- 掌握状态：🟢统一结构化调用的目的；🟢模型语言与后端事实边界；🟢Mock和真实冒烟测试分工；🟡独立完成完整服务迁移和测试设计。
- 下一步：进入阶段2，先定义“粘贴JD并选择简历”所需的请求与返回数据结构，不急着增加Agent工具。

## 2026-09-02 手动JD求职辅助第一版闭环

- 完成功能：新增岗位辅助请求与返回结构、`POST /jobs/assist`、规则评分、简历定制建议、求职招呼语，以及Vue手动粘贴JD页面。
- 数据路线：用户填写简历编号/岗位/公司/JD → Axios携带JWT → FastAPI校验用户简历归属 → 岗位要求分析 → 后端规则评分 → 可信证据过滤 → 招呼语 → Vue展示。
- 用户亲手完成：后端Schema、岗位辅助Service、路由、Vue请求和页面、评分去重、空技能安全招呼语及其回归测试。
- AI处理内容：代码检查、故障定位、学习记录和技术决策整理；核心业务代码由用户亲手修改。
- Bug与修复：前端漏传岗位和公司导致422；安全招呼语曾放入错误函数导致500；完整测试发现统一LLM层会把非1305错误也切到备用模型，恢复为仅1305切换。
- 安全边界：匹配技能为空时不调用模型生成候选人能力描述；简历改写建议只有在模型证据与后端可信经历完全一致时才保留。
- 真实验证：浏览器成功完成手动JD请求并展示匹配分、简历建议和安全招呼语；前端生产构建转换92个模块。
- 验收结果：岗位辅助专项15项、后端完整回归90项全部通过。
- 已知限制：固定技术词表不能覆盖表达、逻辑拆解等通用能力；严格证据相等可能过滤有效建议；页面尚未单独展示没有证据的岗位要求。
- 掌握状态：🟢前后端接口字段一致、局部测试与完整回归的区别；🟡规则评分、可信证据边界；🔴独立设计技术技能与通用能力的分层匹配。
- 下一步：在不降低事实可信度的前提下，设计“技术技能标准化、通用能力单独匹配、证据编号引用”的第二版匹配方案。

## 2026-09-03 简历建议的编号引用与草稿展示

- 本轮完成范围：模型返回`AdviceDraft/TailorDraft`编号草稿；提示词使用`enumerate`给经历编号；`get_proof`检查编号范围和空白经历；`check_draft`取回对应文字并转换成原有`TailorResult`。前端新增待补经历依据列表、引用标签和人工核对提醒。
- 数据路线：当前简历分析中的经历列表 → 编号提示词 → 模型编号草稿 → 后端检查引用并取回经历 → 原有接口返回结构 → Vue展示引用、草稿和待补依据。前端请求格式未改变。
- 用户亲手完成：新增草稿结构、编号循环、取证据与草稿转换函数、调用接线、提示词调整、两个旧测试迁移、页面展示和风险提醒，并亲自运行验证命令。
- AI处理内容：检查实际代码、提供小段代码和测试样板、执行只读诊断与不调用模型的辅助检查、整理记录；本次收尾只改文档及先前过度承诺的Schema注释，不代写业务逻辑。
- 学习与Bug：`rows.append(i,text)`误传两个参数，需要先拼成一个字符串；最后`join(evidence)`仍使用原列表，改为连接编号后的`rows`；切换为草稿结构后，旧模型模拟数据和旧过滤函数也必须同步迁移。
- 自动化验证：用户提供正常调用测试1项通过、无经历时引用0号的异常测试1项通过、岗位辅助文件15项通过；最近一次用户运行的后端完整回归为90项通过，耗时77.61秒。测试数量未增加，本轮迁移了两个既有用例。
- 辅助检查：编号清单、空列表、原始列表不被修改已通过不调用模型的检查；负编号、越界编号和空白经历有本轮手工验证结果，尚未全部变成持久化自动化测试。
- 前端验证：用户截图显示Vite生产构建成功，92个模块，耗时657毫秒；真实页面已展示引用原文、待补依据列表，随后补上草稿风险提示。
- 固定模型样例：两条经历为FastAPI登录接口开发和账号运营；最终返回FastAPI引用原文及相应改写，Docker部署经验进入待补依据列表。先前“建议补充Docker经验”混入改写正文，通过提示词澄清字段职责后此样例改善；不能据此推断普遍可靠。
- 真实失败案例：实际岗位页面中，仅含岗位/公司/日期的“新媒体运营”经历被扩写为“前后端内容更新和维护、优化用户交互体验”；另一条引用未体现的AI工具、自动化与效果描述也被加入草稿。编号可取回，但引用未支持新增事实。
- 已确认边界：编号检查只保证引用来自本次经历列表，不验证`requirement/summary/rewrite`的语义真实性。该列表本身可能是AI从简历提取的内容，不等于原PDF逐字原文或已独立核实的事实；`missing_requirements`也不应解读为用户必然缺少能力。
- 风险处理：页面明确显示“AI改写草稿（待核对）”并提醒用户逐条确认；这不是自动事实校验，也没有实现强制审批或提交阻断。代码注释不再把编号验证后的建议称为已核验事实。
- 未完成事项：改写事实核验、真实失败案例的系统化评测、技术技能标准化和通用能力匹配；浏览器匿名脚本的`startTime`报错来源仍未确定，未修改CORS或其他业务代码来猜测修复。
- 掌握状态：🟡列表、循环、格式化字符串、草稿与返回结构、测试和真实验证的区别，当前能在讲解和示范下修改；尚未验证独立重写能力，不因测试通过就标记已掌握。
- 学习模式：用户确认先跟随完成第一遍；每次先讲页面用途和数据路线，再给5～15行以内核心小段，卡住时直接补示范，不用填空题反复阻塞进度。
- 面试证据：具备编号引用的正常/异常测试、后端90项回归、前端构建、固定模型样例和“引用存在但改写无依据”的真实失败记录。
- 下一步：检查并提交本轮有限范围的改动；事实核验仍保持未完成，不能把该提交描述为已经解决模型编造。

## 2026-09-03 技能别名转换与匹配回归

- 完成范围：用户在`matching_service.py`新增`skill_name`，统一两边空白和大小写，并明确映射`python编程/python语言 → python`、`vue.js → vue`；`calculate_skill_score`的简历和岗位两侧复用此函数。
- 数据路线：两份技能列表 → 逐项名称转换 → 两个集合 → 交集计算匹配项、岗位集合减简历集合计算未匹配项 → 35分技能维度与列表结果 → 原有调用者。没有改变前端接口或增加模型调用。
- 用户亲手完成：名称转换函数、评分函数两处接线、两个新增测试，并自行运行验证；AI负责示范、只读审查与文档整理，本轮未代写核心逻辑。
- Bug与理解：`.lower`忘记括号会得到方法对象，改为`.lower()`才执行转换；函数可以引用写在文件后面的函数，前提是实际调用时其定义已执行；循环中的`skill`是一项字符串，不是整个列表。
- 测试证据：别名用例检查Python与Vue命中、Docker未命中，技能分为23；状态用例检查“正在学习Python”“不会Docker”不会直接匹配Python/Docker。用户运行匹配测试文件13项通过（0.38秒），完整后端92项通过（8.32秒）；相较此前90项新增2项，AI未重复运行全套。
- 手工证据：`Python编程`对`Python`从原先不命中改为技能维度35分；`正在学习Python`只转小写，保留状态文字。35分不是页面总分。
- 边界：这是明确别名的确定性转换，不是删除中文或自动理解语义；只验证了上述样例，不能推断所有学习/否定表达都能处理。仅接入`calculate_skill_score`，其他评分入口的一致性尚需检查；通用能力匹配、原始简历事实核验仍未完成。本轮没有新的浏览器整链路验收。
- 掌握状态：🟡函数调用、列表逐项遍历、字典映射、集合、断言。用户能解释映射并纠正变量理解，但尚无独立重写证据，不因92项通过标记为全面掌握。
- 最新教学约定：测试模板与运行命令可以复制，输入数据与预期断言必须理解；不要求逐行手敲全部测试。核心业务代码第一遍继续由用户在小段示范下输入。
- 面试问题：为什么只统一明确别名，不直接删除技能名称中的中文？
- 唯一下一步：审查并提交本轮限定文件，不夹带`AGENTS.md`、开发总计划、`agent_service.py`或`.vscode/`的其他改动。

## 2026-09-03 补齐名称转换的调用位置

- 上个检查点：用户已提交`0992fbd fix: 统一技能别名并补充匹配测试`。
- 新发现：同样输入`Python编程`，技能维度已命中，但关键词维度仍为0；原因是关键词评分仍只使用`lower()`。必备技能对照与岗位技能合并也仍使用旧处理。
- 用户修改：关键词评分、必备技能两侧和岗位技能合并统一调用`skill_name`，共4行业务替换；保留原有权重和接口结构。
- AI完成：补4个测试，覆盖关键词别名、必备技能别名去重、岗位技能合并和另外两种评分入口的状态描述。修改前运行得到15项通过、2项失败，失败准确指向尚未修改的两个函数。
- 验证：用户运行完整后端得到96项通过（8.41秒）；已读取实际差异确认接线正确，未代写业务代码或重复调用模型。
- 页面路线核对：岗位列表`handleMatch`请求`/jobs/match`，返回值由`JobCard`显示技能分、关键词分、必备技能参考；必备技能15分不重复加入总分，其未匹配项参与解释缺口。定制建议`/jobs/assist`不使用关键词分，不能把两个页面的分数直接比较。
- 未完成边界：尚无本轮真实浏览器匹配结果；通用能力长句、简历事实核验和AI提取的稳定性仍未解决，不承诺所有真实岗位分数都会升高。
- 掌握状态：仍为🟡在指导下复用函数和修改评分逻辑；用户明确希望加快第一遍，不再逐行确认。关联核心修改合并讲解，测试样板可由AI提供，仍需理解输入与断言。
- 提交节奏：按完整关联功能组织，不按每段教学或单个测试提交；本轮暂不安排新的Git提交。
- 唯一下一步：用户在“岗位列表”选择已有已分析简历，对一个岗位执行“开始匹配”，核对三块评分展示并提供截图。

## 2026-09-03 页面请求等待与定制页简历选择

- 用户完成：仅为`/jobs/match`设置120000毫秒前端等待时间，并在`handleMatch`保留`error.message`；定制页复用`fetchMyResumes`，通过`onMounted`加载简历列表，下拉框显示文件名、以编号参与原有分析请求。
- 诊断边界：原全局Axios等待时间为5000毫秒，截图只有OPTIONS成功和本地语义模型加载，没有POST最终结果。调整后用户截图展示了真实匹配结果，支持等待不足的假设，但没有保存精确耗时或原始超时异常，不能声称根因已完全证实。
- 前端验收：AI读取用户改动并运行构建成功，92个模块、188毫秒；用户先确认下拉框显示简历，再提交真实JD，页面展示Python匹配、Agent未匹配、引用、改写草稿、待补信息和招呼语。界面与请求流程已接通，不等于内容真实性通过。
- AI处理：只读核对与构建检查、记录整理；本轮页面核心代码由用户输入。发现`JobList.vue`兜底文字中多了一个`a`（`a岗位匹配失败`），暂未修改，不影响成功路线。
- 新真实失败证据：引用只有“新媒体运营/公司/日期”，草稿却宣称前后端与大模型架构设计和编码；“舆情监测体系搭建”标题被扩写为大模型深度使用及Prompt/Agent模式经验。这些主张没有得到所展示引用的支持，不能据此否认用户实际能力，但不可当成已核实的简历内容。
- 后端核对：`check_draft`仅验证编号并取回经历，随后原样传递`item.rewrite`；`draft_greeting`在存在匹配技能时仍用总结调用模型生成文字，其事实也没有独立核验。
- 当前判断：选择简历的功能闭环通过；自由改写仍未通过可靠性验收，风险提示本身不足以证明输出可信。引用来自简历分析列表，不能宣称已经核对原PDF事实。
- 下一步：与用户确认是否先暂时停用自由改写、保留引用资料和待补信息，再实现更保守的输出。属于影响核心用途的设计选择，未获确认前不擅自修改业务行为，不增加第二次模型审核调用。

## 2026-09-03 暂停自由改写的保守输出

- 推进依据：用户在收到保守版本提议后要求继续学习，并亲手完成对应修改。本轮仅暂停独立改写字段，不取消模型总结、招呼语或规则评分。
- 用户完成：删除`AdviceDraft.rewrite`；提示词仅要求条目提供岗位要求和引用编号；`check_draft`固定返回`rewrite=''`；页面移除改写段落并保留候选经历。
- 数据路线：经历列表 → 模型选择候选编号 → 后端检查编号、取回原列表文字、清空兼容字段 → 页面展示候选引用与待补信息。
- AI完成：迁移2个旧测试样板、加强提示词与空改写断言、新增2个测试；新测试在修改前确实失败。直接整理残留页面标题、旧提示与Schema注释，未代写核心处理。
- 验证：用户运行后端98项通过（8.41秒）；AI核对差异并运行前端构建成功，92个模块。用户随后提供真实页面截图，暂停自由改写提示、候选引用、待补依据和招呼语均可见，自由改写段落已消失，本轮展示目标通过。
- 关键测试：用带虚构架构经历的假模型对象保留额外`rewrite`，验证业务层仍输出空字符串；不是仅依赖Schema忽略额外字段而让测试通过。其他引用和待补字段保持原行为。
- 明确边界：候选编号正确仍不代表能支持岗位要求；经历可能来自AI分析而非逐字PDF。模型总结和有匹配技能时生成的招呼语仍可能失实，需要人工核对。不能称为“全部内容已验证”或“已解决所有幻觉”。
- 掌握状态：🟡理解输出格式、业务约束、页面展示的分工；暂无独立重写证据。测试模板和文案由AI处理以减少机械操作，不按小步安排Git提交。
- 页面证据：用户截图`codex-clipboard-03602a52-e750-435f-a3b9-3ac51e2ae73a.png`显示37分、Python匹配、Agent未匹配；分数不是本轮验收目标。候选引用仍仅有公司/岗位/日期或项目标题，无法凭这些文字证明对应的大模型深度使用等要求。截图不能单独证明接口字段为空，接口约束的证据仍是此前代码核对和测试。
- 本轮AI操作：只读核对简历提取及保存代码、更新记录；未改业务代码、未重跑模型、未安排额外测试或提交。保存函数直接保存`analysis.work_experience`，但尚未比对实际简历提取文本和数据库内容，不能断言经历细节在哪一步丢失。
- 唯一下一步：检查这份简历的已提取原文与已保存经历列表，确认具体工作内容是否在原文中、是否进入AI分析结果；先定位输入资料问题，不直接放宽事实约束或恢复自由改写。

## 2026-09-03 经历细节缺失的只读定位

- 核对对象：用户此前使用的简历编号3；仅查询该简历文件信息和经历字段，未读取账户密码或令牌，未调用外部模型、未写数据库。
- 证据：按PDF读取技能查看两页原简历，并使用应用现有解析函数取得1345字符；原文件及解析文本均包含HTML/CSS品牌展示页、AI工具生成短视频、运营与舆情处理的具体内容。数据库中的经历只有三条标题/岗位/日期，缺少上述具体职责。
- 解析限制：当前PDF文本顺序与可视页面不同，部分公司和项目标题提前集中出现；细节没有全部丢失，但标题与职责的关联顺序可能影响模型提取。不能称为PDF解析完全正确。
- 当前代码路线：完整解析文本进入简历分析提示词；保存函数直接保存`analysis.work_experience`；定制建议直接读取该字段。未发现页面截断经历的逻辑。旧模型响应和当时运行版本未留存，不能断言历史丢失原因已唯一确定。
- 本轮教学安排（尚待用户输入）：在简历分析提示词明确保留具体职责、工具、原有成果及学习状态，归属不明不猜；在“我的简历”的AI分析结果中展示已有`work_experience`字段。共约12行核心内容，不增加模型调用次数或改变返回结构，不恢复自由改写。
- 验证安排：用户完成后运行简历AI服务现有测试和前端构建，再对同一简历执行一次AI深度分析、检查新经历列表。该操作会产生模型费用并替换该简历已保存的分析结果，不修改上传的PDF。测试和构建不证明模型提取内容正确，仍需原文对照。
- 掌握状态：🟡在指导下沿解析、模型、保存、展示路线定位；不因只读检查由AI完成而标为用户独立排错成功。
- 唯一下一步：用户完成上述小段提示词与经历展示修改，反馈“提取的经历”区域结果；暂不提交Git。

## 2026-09-03 经历细节展示通过，技能学习状态仍有丢失

- 用户完成：修改简历提取提示词并在“我的简历”页面循环显示经历，重新分析后提供截图。AI只读核对当前代码和编号3的分析记录，未代写核心代码、未调用真实模型。
- 本次样例验收：已保存经历从此前3条标题变为11条具体描述，含HTML/CSS品牌展示页、AI工具生成营销内容、运营与舆情处理，并保留“目前正在自学Python和RPA”的原文限定。不是仅页面显示变化，数据库内容也已更新；不据此保证所有经历提取完整或所有样例正确。
- 检查结果：AI运行简历AI服务现有4项测试通过（0.96秒），前端构建通过（92模块、163毫秒）；此前完整后端基线为用户报告98项，本轮未重新运行全量回归。
- 新发现：同一结果的`skills`仍含无学习限定的“Python编程”“RPA自动化”“AI Agent开发”。其中“Python编程”经过明确别名表会变成`python`，从而进入技能命中；经历中保留学习状态不能修正已经失去状态的技能字段。
- 下一步教学（待用户输入）：仅完善提示词第2项，明确评分技能需要原文明示使用或掌握依据，单纯学习/计划的内容保留在总结或经历，不列入评分技能；不改评分权重、不靠删别名掩盖输入问题。提示词不是强制事实校验，真实重新分析后仍须核对。
- 控制台边界：截图有匿名VM脚本的`startTime`异常，仅凭截图不能认定来自扩展或业务代码，也不能据此推翻本次结果已返回和保存的证据；本轮未开展该脚本来源定位。
- 项目证据：保留此前缺失细节和本次展开经历的前后对照；学习状态仍为🟡指导下修改与验证。记录已更新，按关联功能批量推进，不逐条提交。

## 2026-09-03 技能学习状态区分的真实样例验收

- 用户完成：修改简历提取提示词第2项并重新执行AI深度分析，提供截图`codex-clipboard-f6d626ef-d01d-4118-aa9c-9fcfc7940b1e.png`。AI只读核对代码及编号3已保存的总结、技能和经历，不再次调用模型或修改核心代码。
- 当前结果：评分技能列表不再包含Python、RPA和AI Agent开发；总结仍明确其为正在学习的内容，经历仍保留自学Python和RPA的限定。运营、内容生产、前端相关技能仍显示。本样例的学习状态区分目标通过，不代表已独立核实所有能力或所有模型输出。
- 经历变化：模型把上次11条细项合并为4条较长描述，仍包含具体运营职责、HTML/CSS展示页、AI工具内容生产和学习状态；条数不是完整性指标，不能因为变少就认定退步，也不能因条数多就认定正确。
- 验证边界：本轮核对真实截图和数据库，未重复运行全量测试；上一轮简历AI服务4项与前端构建通过仍是此前检查记录。提示词没有强制保证，整体评分中的经历关键词规则也未因本次改动自动获得学习状态识别能力。
- 掌握状态：🟡按指导区分评分技能与学习内容；保留真实错误与改正案例作为项目证据，不等于用户已能独立重写。
- 唯一下一步：在定制建议页用同一简历和JD重新分析一次，核对下游已使用更新后的技能和具体经历；不重新执行简历AI分析，不追求分数升高，不恢复自由改写，不按小步提交Git。

## 2026-09-03 新简历分析进入定制建议的闭环验收

- 页面证据：用户截图`codex-clipboard-855d2f23-c8f2-4711-85e9-48d407b09339.png`显示43分，已具备技能为空，待匹配技能包含Agent、Python；引用已是具体运营、HTML/CSS和AI工具经历，保留自学限定，没有自由改写段落。招呼语使用无匹配技能时的固定兴趣表达。本次“新技能与具体经历进入下游”目标通过。
- 质量边界：模型仍把内容运营与HTML/CSS展示页经历关联到AI平台架构、跨境电商架构等要求，同一段经历被反复列作候选；多个要求同时出现在候选区和待补依据区。候选不等于已证实，当前展示容易误读，不能据此确认模型判断可靠。
- 评分核对：`/jobs/assist`为技能35、经历30、方向10三项相加，再除以75换算百分制，技能为空不要求总分为0。经历规则只检查是否共享任意一个词（含AI、Python），方向规则使用AI推荐岗位中的共享方向词；因此可能宽松命中，学习状态也可能通过经历关键词影响评分。本轮没有该次完整JD分析响应及分项记录，不声称精确还原43分组成。
- 验证：AI对当前代码运行完整后端测试，98项通过（10.23秒）；前端上轮已构建成功且本轮未修改。测试通过不代表真实匹配合理，现有评测数据仅3个固定模拟岗位，尚未覆盖此次误关联案例。
- 本轮操作：只读核对服务和评测数据、运行已有测试、更新记录；未修改业务逻辑、未新增模型调用、未提交Git。技能状态和经历提取的当前样例收尾，不继续反复调整提示词。
- 唯一下一步：为定制建议展示技能、经历、方向三项评分来源，使总分可检查；先确定哪里加分，再用真实误判案例调整规则。该功能尚未实现，下一轮继续由用户输入少量核心代码。

## 2026-09-03 定制评分明细教学进行中

- 目标：将同一次规则计算中的技能、经历、方向分数随`JobAssistResponse.parts`返回并展示；保持总分算法、既有字段、模型调用次数不变。
- 计划核心（待用户输入，共约15行新增或修改）：新增`ScoreParts`，三个整数分项分别限制0～35、0～30、0～10；响应要求必填`parts`；`score_job`多返回一个分项对象，`assist_job`接收并传给响应；Vue展示三项及换算说明。
- AI已完成：仅更新两个测试文件中的旧构造、解包和假对象，断言分数分项一致及API返回分项；新增6组越界测试和1个零分/满分边界测试。高总分测试指定错误字段，避免因缺少分项而假通过。
- 初始状态：生产代码未新增`ScoreParts`时，直接导入检查报`ImportError`，属于测试依赖待实现，不能算完成。
- 用户后续完成：使用自己起的类名`Scoreminxi`完成Schema、服务导入/返回/接收/响应及Vue分项展示；保留该名称，不强制恢复`ScoreParts`。`parts`为分项，`skill/exp/role`分别为技能/经历/方向。
- 导入报错根因：业务代码已经统一为`Scoreminxi`，AI准备的两个测试文件仍引用`ScoreParts`。AI已将测试导入与构造同步为用户类名，未改生产代码。
- 随后验证：两个专项测试26项通过、1项失败（9.42秒）；唯一失败为0分边界。用户把`skill`约束写成`Field(gt=0,le=35)`，拒绝实际业务合法的0分，应由用户改成`Field(ge=0,le=35)`；不应删除边界测试或把0分伪装为1分。
- 学习重点：Schema声明字段不会自动搬运函数局部变量；必须计算、返回、接收、组装响应、页面读取全部接通。分项必填、不默认填零，避免漏传被掩盖。
- 唯一下一步：用户在`schemas.py`的`Scoreminxi.skill`中将`gt=0`改为`ge=0`，再运行两个专项测试；明细功能尚待完整检查和页面验收，不修改评分权重或重复调用模型，不安排本轮Git提交。

## 2026-09-03 评分明细验收与共享词误判反例

- 用户完成：将技能分边界改为`ge=0`，并提供真实页面截图`codex-clipboard-335e0895-446d-4047-ad30-9193b79e5344.png`。页面显示技能0、经历22、方向10，总分43；(0+22+10)/75*100约为42.67，取整43。评分明细传递和展示目标通过，不代表规则评分准确性通过。
- AI检查：当前完整后端105项通过（9.53秒），前端构建通过（92模块、189毫秒）。只读诊断及现有测试，没有模型调用、业务修改或Git提交。
- 确定性反例1：直接调用经历评分，输入经历“运用AI工具批量生成营销短视频”、职责“负责AI平台架构设计”，得到经历满分30并引用该经历。当前规则因共享AI一词便命中，无法证明职责成立。该独立反例不是截图22分的逐条还原。
- 确定性反例2：岗位“AIAgent全栈开发工程师”与推荐方向“AI驱动的新媒体运营专家”得到方向10分，原因仅为共同方向AI。两个反例不需要大模型即可重复验证。
- 其他已记录问题：本次待补依据包含福利、联系方式和页面杂文，并非都属于能力要求；模型候选关联仍不可靠。与经历规则问题分开排期，不在本轮叠加提示词或批量修改所有模块。匿名VM脚本报错来源仍未确认。
- 项目证据：分项截图、正常及边界回归、两个离线误判反例；用户核心修改完成并在指导下验收，独立解释和重写能力仍为🟡。
- 唯一下一步：先把AI内容工具与AI架构职责的误命中作为回归反例，再由用户收紧经历匹配规则；明确规则只能提供线索而非自动证明事实，不直接调低总分或恢复自由改写。

## 2026-09-03 经历宽泛词回归测试已补，核心修改待用户完成

- AI新增测试：7个只共享AI、接口、数据库、部署、测试、需求、文档的误命中案例；3个具体技术词正常命中案例；2个空经历边界。未修改生产逻辑。
- 旧规则验证：匹配专项与固定岗位评测共30项，7项失败、23项通过（0.51秒）；7个反例均错误获得30分。失败是新测试暴露既有问题，不是功能已修好。
- 拟由用户修改：`matching_service.py`顶部将`EXPERIENCE_KEYWORDS = JOB_KEYWORDS + [...]`改为`EXPERIENCE_KEYWORDS = JOB_KEYWORDS`，不改变任意共同词的判断方式、满分30及总分权重。
- 方案检查：仅在独立测试进程中临时绑定新词表，同一30项全部通过（0.04秒），原3岗位排名不变；此检查未写入业务文件，不能替代用户保存后的测试。
- 当前边界：仅撤销宽泛词单独作为命中依据；缺少明确技术词的真实经历也可能漏匹配，学习/否定状态、同技术不同职责、方向AI误判仍未解决。规则零分表示没有命中当前规则，不表示人一定不会。
- 学习状态：🟡继续学习用反例验证规则；唯一下一步为用户亲手修改这一行并运行上述30项测试。本轮不提交Git、不调用模型。

### 用户完成与代码回归结果

- 用户已亲手保存`EXPERIENCE_KEYWORDS = JOB_KEYWORDS`及中文注释，报告专项30项通过（0.41秒）。AI读取实际文件确认修改，不再只是进程内模拟。
- AI运行完整后端测试：117项通过（9.21秒）。7个宽泛词误判反例现在不加分，具体技术词、空输入及原3个固定岗位排序均通过；经历满分和总分公式不变。
- 验收范围：规则及现有代码回归通过，修改后真实页面尚待复查；没有运行前端构建（本轮前端未修改），没有新增AI调用或提交Git。
- 数据路线：定制页请求→jobs接口→assist_job/score_job→经历规则→Scoreminxi分项与总分→原响应→Vue评分明细。已有岗位匹配接口也复用经历规则，完整测试包括相关调用。
- 唯一下一步：用户在定制建议页面使用同一简历、同一岗位JD重新分析一次，仅提供顶部评分明细截图；不重新分析简历，不反复调用模型追求固定分数。学习状态/否定和同技术不同职责、方向共享AI等边界仍未解决。独立解释能力仍为🟡，本轮面试题为“为什么只给函数补正常情况测试不够？”

## 2026-09-03 经历页面复查与方向依据修正教学

- 用户截图`codex-clipboard-884055d4-e5a7-4f51-a545-574c06ff1a6b.png`显示技能0、经历0、方向10、总分13；(0+0+10)/75*100取整为13。相较此前经历22、总分43，页面结果符合本轮修复预期；没有完整两次JD模型输出，不能把变动逐项归因或宣称所有匹配正确。经历宽泛词回归和本次页面复查收尾。
- 方向检查：只读查询既定简历3的推荐方向，内容为AI驱动的新媒体运营专家、数据分析师、全栈开发工程师。直接调用当前`score_role`与截图岗位标题得到10分、说明“共同方向:AI”。因此不能把当前10分一概视为不该得分：输入确实包含全栈方向，但旧词表漏掉全栈，只命中了宽泛AI。
- 下一段核心待用户输入：将`ROLE_KEYS`中的AI换成全栈；保留方向10分权重。输入为岗位标题和AI推荐岗位名称，输出仍是方向分、是否命中及说明；推荐方向不是实际经历证据。
- AI已新增6组测试：仅AI运营不命中、当前推荐列表按全栈命中、单独全栈也命中、产品及Vue前端正常、空方向不命中。当前业务代码下专项3失败/33通过（0.61秒）；独立进程临时替换词表后36通过（0.05秒），未改业务文件，未运行新的完整后端回归。
- 已知边界：有限关键词不能覆盖所有职业或判断胜任能力；模型候选引用仍和规则分项走不同生成路径，经历0不会自动清空候选引用。页面匿名VM startTime报错仍在，来源未确认，本轮不作浏览器扩展归因。
- 唯一下一步：用户亲手修改方向词表并运行匹配专项与固定岗位评测，预期36通过。不重复调用AI追求分数变化；当前列表正确识别全栈后仍可得方向10分。知识掌握仍为🟡，不按单行修改提交Git。

## 2026-09-04 岗位报告与历史恢复验收

- 完成：从JD提取要求，与PDF解析文本逐条对照；展示判断、理由和编号引用。
- 完成：报告保存、最近20份历史列表、按当前用户查询详情、刷新后恢复表单与报告。
- 用户亲手完成：数据模型、业务函数、接口、前端展示及修复；按指导运行测试。
- AI辅助：代码审查、原因定位、测试样板和教学说明；本次收尾未代改业务代码。
- Bug：真实报告表只有id；create_all不更新已有表。确认旧表为空后保留旧表并创建完整表。
- Bug：模型判定有支持却没有引用；改为待核对并替换理由，其他结构检查保留。
- Bug：终端中文编码变成问号；统一UTF-8。理由误写为集合；改回字符串。
- 验证：报告保存与权限测试2项通过；无引用降级3个本地案例通过；浏览器刷新后可打开历史。
- 边界：引用存在不等于语义正确；本轮没有重新运行全量回归。
- 掌握状态：正在学习，已在指导下修改和排错，尚未验证独立重写能力。
- 下一步：整理关联改动并提交报告功能，然后进入投递状态跟踪。

## 2026-09-05 投递状态跟踪第一版

- 完成：从历史报告加入投递管理，查询自己的记录，修改状态和备注。
- 用户亲手完成：数据模型、请求模型、业务函数、接口、Vue页面及导航。
- AI辅助：代码审查、测试样板、报错解释及验证步骤。
- 验证：投递与报告相关测试3项通过；前端构建通过；页面显示记录并返回保存成功。
- 测试覆盖：重复创建、列表隔离、更新保存、越权修改及不存在记录。
- Bug：标题误标成公司字段；前端更新地址遗漏复数s。语法检查不能发现这些业务错误。
- 边界：当前按报告编号去重；不同报告可能对应同一岗位。并发创建尚未专项验证。
- 已知问题：浏览器匿名脚本startTime报错来源仍未确认。
- 掌握状态：正在学习，能在指导下实现和排错，尚未验证独立重写。
- 下一步：提交投递模块，结合路线图确定下一项工作。

## 2026-09-05 匹配规则修正收尾

- 完成：统一技能别名入口，收紧经历匹配词表，方向匹配删除宽泛AI并保留全栈。
- 用户亲手完成：业务规则修改、保存和测试运行；AI辅助审查与测试样板。
- 验证：匹配专项35项通过；加固定岗位评测共36项通过，无真实模型调用。
- 边界：本轮未运行完整后端回归；有限关键词匹配不代表能力或事实核验。
- 排错：停止DevTools实时性能采集后，导航不再新增startTime错误；属于临时处理。
- 掌握状态：正在学习，尚未验证独立重写。
- 下一步：提交这组评分修正，再开始记录真实岗位使用效果。

## 2026-09-05 浏览器扩展安全演练验收

- 完成：扩展从源码硬编码JWT改为通过弹窗保存到`chrome.storage.local`，并保持`DRY_RUN=true`、每日上限1和人工确认。
- 用户亲手完成：Token变量修正、DOM监听防抖、重复提示保护，以及已打开首个岗位的JD等待边界修复。
- 数据路线：BOSS岗位卡片 → 快速规则评分 → 达标岗位读取完整JD → 后端报告精判 → 阈值判断 → 演练结果；本轮没有真实投递。
- 验证：新Token鉴权成功；15个真实岗位完成快速评分；3个达标岗位均完成精判。当前样例最高为2条有依据，低于`APPLY_MIN=3`，全部被正确拦截。新增快速评分单元测试后，完整快速回归为135项通过、1项慢测试跳过（8.72秒）。
- Bug：监听器观察到插件自己反复修改提示文字，形成循环并卡住页面；增加“文字变化后才更新”和500毫秒防抖后恢复。
- Bug：首个岗位已经打开时JD不变化，被`waitJd(prev)`静默跳过；根据当前标题选择等待基准后，三个目标均显示精判结果。
- 安全检查：扩展脚本语法与Manifest解析通过；未发现硬编码JWT；真实投递仍未开放。工作区还有大量未整理改动，禁止直接使用`git add .`。
- 掌握状态：🟡能按证据完成路径、401鉴权、监听循环和异步边界排错，尚未独立重写扩展流程。
- 下一步：整理插件关联改动和提交边界；在可靠性不足前保持演练模式，不启用真实自动投递。

## 2026-09-05 LLM限流与备用模型验收

- 完成：结构化模型请求固定`temperature=0`，降低岗位分析和报告输出的随机波动。
- 完成：主模型抛出`RateLimitError`且已配置备用模型时切换一次；普通异常不隐藏，没有备用模型时保留原异常。
- 兼容调整：删除旧模型私有错误码`1305`判断，改为依赖OpenAI兼容SDK的限流异常类型，切换模型供应商时无需同步修改私有码。
- 用户亲手完成：异常处理最小修改、测试假异常改名及引用同步；AI辅助定位`NameError`和执行完整回归。
- Bug：只修改了`OtherError`的引用，没有同步修改类定义，触发`NameError`；统一定义和引用后修复。
- 验证：LLM专项7项通过；完整后端135项通过、1项慢测试跳过（8.32秒）；测试使用假响应，没有调用真实模型或产生费用。
- 边界：`temperature=0`只能降低随机性，不能保证输出完全相同；备用模型失败仍会把异常交给上层处理。
- 掌握状态：🟡能根据`NameError`同步修复定义与引用，备用模型异常路线尚未独立重写。
- 下一步：只提交LLM稳定性相关服务、测试和本学习记录，不混入RAG、数据库初始化或前端改版。

## 2026-09-05 RAG证据充分性修正

- 完成：真实检索来源继续覆盖模型返回的来源，但不再用“来源非空”强制覆盖模型的`enough`判断。
- 原因：检索到相似片段只表示有候选资料，不代表资料足以回答用户的具体问题；二者不能使用同一个布尔判断代替。
- 用户亲手完成：新增“有来源但模型判断资料不足”的回归测试，并删除`result.enough=len(sources)>0`覆盖语句。
- 验证：RAG快速测试15项通过、2项慢测试未选择（6.90秒）；最终完整后端136项通过、1项慢测试跳过（8.34秒），没有调用真实模型。
- 数据边界：`result.sources=sources`仍然保留，用系统实际检索来源替换模型可能生成的来源；`enough`保留结构化模型判断。
- 掌握状态：🟡理解“检索命中”和“证据充分”不是同一概念，尚未独立设计更严格的充分性评测。
- 下一步：只提交RAG服务、RAG测试和本学习记录，不混入数据库初始化与前端改版。

## 2026-09-06 SQLite迁移到MySQL 8.4验收

- 完成：安装并配置MySQL Community Server 8.4.11；创建`ai_job_agent`数据库和仅限本地、仅限该库的`job_app`应用账号，应用不使用`root`连接。
- 完成：数据库地址改由`DATABASE_URL`环境变量读取；SQLite才传入`check_same_thread=False`，MySQL通过`PyMySQL[rsa]`连接；为MySQL要求的11个`String`字段补充长度。
- 完成：测试环境通过`tests/conftest.py`切换到项目`tmp/pytest.db`，避免测试读写开发MySQL；初始化岗位改为可重复执行，重复运行不会增加岗位数量。
- 用户亲手完成：数据库安装与账号配置、连接验证、模型字段长度修改、数据库配置切换、测试隔离、迁移函数及`flush`修复，并运行完整测试。
- 数据迁移：迁移前复制`jobs_before_mysql_20260906.db`备份；从SQLite迁移45条数据到MySQL。最终数量为岗位3、用户4、简历3、分析2、报告30、投递3。
- Bug：第一次连接`job_app`出现1045，重设应用账号密码后解决；SQLite专用参数拼写错误导致MySQL连接收到`check_same_thread`，修正拼写并只对SQLite传入；首次迁移出现MySQL 1452外键错误，在每张表后`flush()`父表数据、所有表完成后统一`commit()`解决。
- 事务证据：失败迁移后MySQL仍保持岗位3、其余表0，证明`rollback()`没有留下半迁移数据；加入`flush()`的试运行可读到全部45条，随后主动回滚，再正式迁移。
- 验证：完整后端137项通过、1项跳过（9.68秒）；实际运行配置显示数据库后端为MySQL、库名为`ai_job_agent`；`/health`和`/jobs`均返回200，岗位数3，中文标题精确比较为真。
- 安全与边界：`.env`和SQLite备份不提交Git；密码不写入文档或终端输出。当前是一次性小数据迁移脚本，不替代正式版本化Migration工具，也不应在MySQL产生新业务数据后反复用旧SQLite覆盖。
- 掌握状态：🟡正在学习。已能跟随证据处理连接、兼容、外键和事务问题；尚未连续两次独立完成数据库迁移与解释，不能标为已掌握。
- 唯一下一步：审查并只提交本次MySQL相关文件，排除工作区中已有的前端修改；提交后进入Redis基础学习。
