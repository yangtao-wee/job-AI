# AI 求职 Agent 开发计划

> 项目定位：这是一个学习型、可逐步扩展的全栈 AI 项目。开发时先完成最小可用版本（MVP），再增加 Agent 与浏览器插件。每个阶段都应由你先理解、设计并编码，我只负责讲解、提问、审查和排错，不直接交付完整实现。

## 第一部分：项目目标

### 1.1 这个项目解决什么问题

求职者通常要反复阅读岗位描述（JD）、判断自身匹配程度、针对岗位修改简历，并组织与招聘者沟通的话术。这个过程耗时、标准不统一，而且很容易遗漏岗位中的关键要求。

本项目要做一个 AI 求职助手，把这些任务串成一个清晰流程：

- 从 PDF 简历中提取结构化信息，如教育、经历、技能和项目。
- 从 JD 中识别职责、硬性条件、加分项和关键词。
- 对简历与岗位进行可解释的匹配评分，而不是只给一个模糊数字。
- 指出证据充分项、缺失项和表达薄弱项。
- 在不虚构经历的前提下，针对岗位优化简历表达。
- 生成简洁、个性化的 BOSS 直聘打招呼话术。
- 后续通过 Chrome 扩展辅助采集用户正在浏览的岗位信息。

项目不应该替用户伪造经验，也不应把 AI 输出当成事实。优化建议必须能追溯到原简历和 JD，最终内容由用户确认。

### 1.2 用户流程

1. 用户注册或登录系统。
2. 用户上传 PDF 简历。
3. 后端保存文件并提取 PDF 文本。
4. AI 将简历文本分析为结构化数据，用户可检查和修正。
5. 用户粘贴目标岗位 JD。
6. AI 提取岗位职责、必备条件、加分项和关键词。
7. 系统根据确定性规则与 AI 语义判断生成匹配分数和评分依据。
8. 用户查看能力匹配、经验匹配、教育匹配、关键词覆盖和风险提示。
9. AI 给出优化建议，并生成针对该岗位的简历表达候选稿。
10. AI 根据简历事实与岗位特点生成 BOSS 打招呼话术。
11. 用户保存分析记录；后续可比较不同岗位的匹配结果。
12. 扩展阶段中，Chrome 插件读取当前岗位页面，经用户确认后发送到系统分析。

### 1.3 MVP 边界

第一版只支持单用户常规流程、文本型 PDF 和手动粘贴 JD。暂不处理扫描件 OCR、复杂排版复原、自动投递、自动向招聘者发消息、多租户与大规模并发。先保证完整链路可运行、结果可解释、数据不混乱。

---

## 第二部分：技术架构

### 2.1 总体结构

```text
Vue 3 页面
    │ HTTP / JSON / multipart/form-data
    ▼
FastAPI 路由层
    ▼
业务服务层 ─────► DeepSeek API
    │
    ├───────────► PDF 文件存储
    ▼
SQLite 数据库
```

前后端应通过明确的 API 契约通信。路由层不堆积业务逻辑，AI 调用不直接写在接口函数中，数据库模型也不直接作为接口返回值。

### 2.2 Frontend：Vue 3

推荐使用 Vue 3、Vite、Vue Router、Pinia 和 Axios。

职责：

- 展示登录、简历上传、JD 输入、匹配报告和历史记录页面。
- 管理表单状态、登录状态、加载状态和错误提示。
- 调用后端 API，但不保存 DeepSeek 密钥，也不执行可信的权限判断。
- 将评分依据、缺失项和修改建议以容易理解的方式呈现。
- 在提交前做基础校验，在收到后端错误后给用户清晰反馈。

边界：前端校验是为了体验，后端校验才是安全边界。所有 AI 密钥、鉴权逻辑和核心业务规则都留在后端。

### 2.3 Backend：FastAPI

职责：

- 提供用户、简历、岗位、分析和生成内容等 REST API。
- 校验请求参数、文件类型、文件大小和用户权限。
- 处理 JWT 身份认证。
- 协调数据库、PDF 解析器、匹配服务和 DeepSeek 客户端。
- 统一处理错误、日志和返回格式。
- 确保用户只能访问自己的数据。

建议遵循“router 接收请求—service 执行业务—repository/ORM 读写数据”的方向。学习阶段可以不单独增加 repository 层，但不得让所有代码挤进 `main.py`。

### 2.4 Database：SQLite（学习阶段）

职责：

- 保存用户、简历元数据、解析文本、岗位 JD、分析结果和生成记录。
- 通过主键、外键和唯一约束维持数据关系。
- 支持用户查看历史分析和重复使用简历。

SQLite 适合本地学习和小规模 MVP：部署简单、无需单独数据库服务。它不适合高并发写入；将来可迁移到 PostgreSQL。建议使用 SQLAlchemy 学习 ORM，使用 Alembic 管理表结构迁移，而不是每次手工删库重建。

### 2.5 AI：DeepSeek API

职责：

- 将非结构化简历文本提取为约定的数据结构。
- 将 JD 拆解为职责、必备项、加分项和关键词。
- 辅助判断语义层面的匹配关系并说明依据。
- 生成忠于原始经历的改写建议和沟通话术。

关键原则：

- API Key 只存于后端环境变量，绝不能提交到 Git 或发给前端。
- Prompt 明确角色、输入、规则和 JSON 输出结构。
- 用 Pydantic 校验模型输出；解析失败时应重试或返回可理解的错误。
- 保存模型名称、Prompt 版本和原始输入摘要，便于复现结果。
- 匹配评分采用“规则计算为主、AI 语义分析为辅”的混合方式，避免模型随意给分。
- 明确禁止虚构经历、技能、数字和成果。

### 2.6 一次分析请求的数据流

1. 前端提交简历 ID 和 JD 文本。
2. 后端验证 JWT、数据归属和输入长度。
3. JD 服务提取结构化岗位要求。
4. 匹配服务把简历结构与岗位结构逐项比较。
5. 规则模块计算各维度分数，AI 补充语义匹配与解释。
6. 后端校验结果结构并写入 SQLite。
7. 前端获得报告并展示总分、分项分、证据、缺口与建议。

---

## 第三部分：完整目录结构

下面是目标结构，不要求第一天全部创建。只在对应阶段创建需要的文件。

```text
ai_job_agent/
├─ frontend/
│  ├─ public/
│  │  └─ favicon.ico
│  ├─ src/
│  │  ├─ api/
│  │  │  ├─ http.js
│  │  │  ├─ auth.js
│  │  │  ├─ resumes.js
│  │  │  ├─ jobs.js
│  │  │  └─ analyses.js
│  │  ├─ assets/
│  │  │  └─ base.css
│  │  ├─ components/
│  │  │  ├─ AppHeader.vue
│  │  │  ├─ ResumeUploader.vue
│  │  │  ├─ JobDescriptionForm.vue
│  │  │  ├─ ScoreCard.vue
│  │  │  └─ SuggestionList.vue
│  │  ├─ router/
│  │  │  └─ index.js
│  │  ├─ stores/
│  │  │  ├─ auth.js
│  │  │  └─ analysis.js
│  │  ├─ views/
│  │  │  ├─ LoginView.vue
│  │  │  ├─ RegisterView.vue
│  │  │  ├─ DashboardView.vue
│  │  │  ├─ ResumeView.vue
│  │  │  ├─ AnalysisView.vue
│  │  │  └─ HistoryView.vue
│  │  ├─ App.vue
│  │  └─ main.js
│  ├─ .env.example
│  ├─ index.html
│  ├─ package.json
│  └─ vite.config.js
├─ backend/
│  ├─ app/
│  │  ├─ core/
│  │  │  ├─ config.py
│  │  │  └─ security.py
│  │  ├─ models/
│  │  │  ├─ user.py
│  │  │  ├─ resume.py
│  │  │  ├─ job.py
│  │  │  └─ analysis.py
│  │  ├─ schemas/
│  │  │  ├─ auth.py
│  │  │  ├─ resume.py
│  │  │  ├─ job.py
│  │  │  └─ analysis.py
│  │  ├─ routers/
│  │  │  ├─ auth.py
│  │  │  ├─ resumes.py
│  │  │  ├─ jobs.py
│  │  │  └─ analyses.py
│  │  ├─ services/
│  │  │  ├─ pdf_service.py
│  │  │  ├─ deepseek_service.py
│  │  │  ├─ resume_service.py
│  │  │  ├─ jd_service.py
│  │  │  ├─ matching_service.py
│  │  │  └─ generation_service.py
│  │  ├─ prompts/
│  │  │  ├─ resume_analysis.py
│  │  │  ├─ jd_analysis.py
│  │  │  ├─ resume_optimization.py
│  │  │  └─ greeting.py
│  │  ├─ dependencies.py
│  │  ├─ database.py
│  │  └─ main.py
│  ├─ tests/
│  │  ├─ test_auth.py
│  │  ├─ test_pdf_service.py
│  │  └─ test_matching_service.py
│  ├─ uploads/
│  │  └─ .gitkeep
│  ├─ .env.example
│  ├─ alembic.ini
│  ├─ requirements.txt
│  └─ README.md
├─ extension/
│  ├─ manifest.json
│  ├─ popup.html
│  ├─ popup.js
│  ├─ content.js
│  ├─ background.js
│  └─ styles.css
├─ .gitignore
├─ README.md
└─ AI_JOB_AGENT_DEVELOPMENT_PLAN.md
```

### 3.1 根目录文件

- `README.md`：说明项目用途、运行方式、环境变量和当前功能。
- `.gitignore`：排除虚拟环境、依赖目录、数据库、上传文件、缓存和密钥文件。
- `AI_JOB_AGENT_DEVELOPMENT_PLAN.md`：记录路线、学习目标、阶段验收和开发约定。

### 3.2 Frontend 文件说明

- `public/favicon.ico`：站点图标，不经过模块打包处理。
- `src/main.js`：Vue 应用入口，挂载根组件并注册 Router、Pinia。
- `src/App.vue`：全局页面外壳和路由出口。
- `src/assets/base.css`：全局基础样式与设计变量。
- `src/api/http.js`：创建 Axios 实例，配置后端地址、Token 请求头和统一错误处理。
- `src/api/auth.js`：封装注册、登录和当前用户接口。
- `src/api/resumes.js`：封装简历上传、查询和删除接口。
- `src/api/jobs.js`：封装 JD 创建与查询接口。
- `src/api/analyses.js`：封装匹配分析、简历优化和话术生成接口。
- `src/components/AppHeader.vue`：导航栏和登录状态入口。
- `src/components/ResumeUploader.vue`：选择 PDF、前端校验、上传进度和结果反馈。
- `src/components/JobDescriptionForm.vue`：输入 JD 并执行基础校验。
- `src/components/ScoreCard.vue`：展示总分、分项分数和评分说明。
- `src/components/SuggestionList.vue`：展示缺口、证据和修改建议。
- `src/router/index.js`：定义页面路由和需要登录的路由守卫。
- `src/stores/auth.js`：管理用户、Token 和登录状态。
- `src/stores/analysis.js`：管理当前简历、JD、分析结果和加载状态。
- `src/views/LoginView.vue`：登录页面。
- `src/views/RegisterView.vue`：注册页面。
- `src/views/DashboardView.vue`：用户工作台和主要流程入口。
- `src/views/ResumeView.vue`：上传简历并查看解析结果。
- `src/views/AnalysisView.vue`：展示匹配报告、优化建议和话术。
- `src/views/HistoryView.vue`：查看以前的岗位与分析记录。
- `.env.example`：展示前端可配置变量名，例如 API 基础地址，不包含真实秘密。
- `index.html`：Vite 的 HTML 入口。
- `package.json`：记录依赖和开发、构建、测试脚本。
- `vite.config.js`：Vite 配置，可配置本地代理和构建选项。

### 3.3 Backend 文件说明

- `app/main.py`：FastAPI 入口，创建应用、配置 CORS、注册路由和异常处理。
- `app/database.py`：创建数据库引擎、会话工厂和 ORM 基类，管理数据库连接生命周期。
- `app/dependencies.py`：集中放置数据库会话、当前登录用户等 FastAPI 依赖。
- `app/core/config.py`：从环境变量读取数据库地址、JWT 配置、上传限制和 DeepSeek 配置。
- `app/core/security.py`：密码哈希、密码验证、JWT 创建与解析。
- `app/models/user.py`：定义用户数据库表及关系。
- `app/models/resume.py`：定义简历文件信息、原始文本、结构化结果和所属用户。
- `app/models/job.py`：定义 JD 原文、结构化要求和所属用户。
- `app/models/analysis.py`：定义匹配分数、证据、建议、生成内容及关联记录。
- `app/schemas/auth.py`：定义注册、登录、Token、用户返回数据格式。
- `app/schemas/resume.py`：定义简历上传结果、解析结构和查询返回格式。
- `app/schemas/job.py`：定义 JD 创建、分析结果和返回格式。
- `app/schemas/analysis.py`：定义匹配请求、分项评分、证据、建议和生成结果格式。
- `app/routers/auth.py`：用户注册、登录和身份查询接口。
- `app/routers/resumes.py`：简历上传、列表、详情和删除接口。
- `app/routers/jobs.py`：JD 创建、列表和详情接口。
- `app/routers/analyses.py`：执行匹配、查询报告、优化简历和生成话术接口。
- `app/services/pdf_service.py`：校验并读取 PDF，返回文本和解析错误。
- `app/services/deepseek_service.py`：统一封装 DeepSeek 请求、超时、重试和结构化响应解析。
- `app/services/resume_service.py`：编排简历保存、文本提取、AI 分析和持久化。
- `app/services/jd_service.py`：清洗 JD，并提取结构化岗位要求。
- `app/services/matching_service.py`：逐项匹配、计算确定性分数并生成可解释结果。
- `app/services/generation_service.py`：基于事实生成简历改写候选和沟通话术。
- `app/prompts/resume_analysis.py`：简历结构化分析 Prompt 及版本标记。
- `app/prompts/jd_analysis.py`：JD 结构化分析 Prompt 及版本标记。
- `app/prompts/resume_optimization.py`：简历优化 Prompt，明确不得虚构。
- `app/prompts/greeting.py`：BOSS 打招呼话术 Prompt 和长度、语气约束。
- `tests/test_auth.py`：验证注册、登录、错误密码和受保护接口。
- `tests/test_pdf_service.py`：验证正常 PDF、空 PDF、错误格式等边界。
- `tests/test_matching_service.py`：用固定输入验证评分可重复、边界正确。
- `uploads/.gitkeep`：保留本地上传目录；实际简历文件不得提交 Git。
- `.env.example`：列出后端环境变量名和安全示例。
- `alembic.ini`：数据库迁移工具配置。
- `requirements.txt`：固定 Python 依赖及版本。
- `README.md`：后端安装、启动、迁移和测试说明。

### 3.4 Chrome 插件文件说明

- `manifest.json`：扩展清单，声明 Manifest V3、权限、脚本和弹窗。
- `popup.html`：用户点击扩展图标后的界面。
- `popup.js`：控制弹窗交互、预览和提交岗位信息。
- `content.js`：在岗位页面读取可见 DOM 内容；不同网站的解析规则应隔离。
- `background.js`：Service Worker，负责消息转发、状态与跨页面生命周期逻辑。
- `styles.css`：插件弹窗样式。

插件只申请完成任务所需的最小权限。采集和发送页面内容前必须让用户知情，不自动投递、不自动发消息。

---

## 第四部分：开发阶段

## 阶段 1：项目初始化

**学习目标**

- 理解前后端分离、端口、HTTP 请求、JSON 和 CORS。
- 掌握 Vue 3/Vite 与 FastAPI 的最小启动过程。
- 学会使用虚拟环境、依赖清单和环境变量。

**需要你写**

- 根目录 `README.md`、`.gitignore`。
- `frontend/` 的 Vite 基础项目、`main.js`、`App.vue`、基础路由。
- `backend/app/main.py`、`backend/requirements.txt`、`.env.example`。
- 一个后端 `/health` 接口和一个调用它的前端页面。

**完成标准**

- 前端与后端可分别启动。
- 浏览器页面能请求 `/health` 并显示结果。
- 能解释 CORS 为什么出现、代理与直接跨域调用有什么差别。
- Git 中没有虚拟环境、`node_modules` 或密钥。

## 阶段 2：用户系统

**学习目标**

- 理解关系型数据库、表、主键、唯一约束、事务和 ORM。
- 理解请求 Schema 与数据库 Model 的区别。
- 理解密码哈希与 JWT 的签发、携带、验证、过期过程。

**需要你写**

- `database.py`、用户 Model、认证 Schema。
- `security.py`、数据库与当前用户依赖。
- 注册、登录、获取当前用户接口。
- 登录/注册页面、认证 Store、Axios Token 拦截逻辑。
- 用户系统的基础测试。

**完成标准**

- 邮箱或用户名不可重复。
- 数据库不保存明文密码。
- 正确账号能取得 Token，错误密码返回合适状态码。
- 无 Token、伪造 Token、过期 Token 无法访问受保护接口。
- 用户刷新页面后，登录状态处理符合你的设计并能解释安全取舍。

## 阶段 3：简历上传

**学习目标**

- 理解 `multipart/form-data`、文件流、文件元数据和服务端校验。
- 理解文件名不可信、路径穿越、大小限制和随机存储名。
- 区分数据库元数据与磁盘二进制文件。

**需要你写**

- Resume Model 与 Schema。
- `resumes.py` 上传、列表和详情接口。
- `resume_service.py` 的保存逻辑。
- `ResumeUploader.vue` 和前端 API 封装。

**完成标准**

- 只允许符合限制的 PDF，错误类型和超大文件会被拒绝。
- 文件使用服务器生成的唯一名称，不能由原始文件名决定存储路径。
- 数据库记录文件所属用户，用户不能读取他人简历。
- 上传失败时不会留下不一致的数据库记录或半成品文件。

## 阶段 4：PDF 解析

**学习目标**

- 理解 PDF 是页面描述格式，不等同于有顺序的纯文本。
- 学会使用 Python PDF 库逐页提取文本并处理空页、乱码和异常。
- 认识文本型 PDF 与扫描件 PDF 的区别，以及 OCR 的适用场景。

**需要你写**

- `pdf_service.py`。
- 解析状态字段和错误信息。
- 上传后触发解析的业务流程。
- `test_pdf_service.py` 与几类不含隐私的测试样本。

**完成标准**

- 能解析普通文本型 PDF，保留合理换行和页信息。
- 加密、损坏、无文本 PDF 得到明确状态，不导致服务崩溃。
- 原始文本与结构化 AI 结果分开存储。
- 能说明为什么第一版不急于支持 OCR。

## 阶段 5：AI 接口

**学习目标**

- 理解大模型 API 的消息结构、Token、温度、超时、限流和费用。
- 理解 Prompt 的目标、上下文、约束、示例和输出格式。
- 理解结构化输出仍然不可信，必须经过 Schema 校验。

**需要你写**

- `config.py` 中的 DeepSeek 配置。
- `deepseek_service.py` 的最小客户端封装。
- 简历分析与 JD 分析 Prompt。
- 对应的 Pydantic 输出 Schema、失败处理和测试替身（mock）。

**完成标准**

- Key 只从后端环境变量读取。
- 同一调用入口具备超时、有限重试和清晰异常。
- AI 返回可以通过 Pydantic 验证，异常 JSON 不会直接写入正式字段。
- Prompt 明确“不知道就留空、不得编造”。
- 测试时不依赖每次真实调用付费 API。

## 阶段 6：岗位匹配

**学习目标**

- 理解结构化数据、归一化、集合比较、权重和评分上限。
- 区分硬性条件、加分条件与普通关键词。
- 理解可解释评分、确定性算法与模型判断的边界。

**需要你写**

- 匹配输入与输出 Schema。
- `matching_service.py` 中的纯函数评分核心。
- 分项权重、证据列表、缺失项和风险项。
- 分析接口、报告页面和固定测试用例。

**建议的第一版评分维度**

- 核心技能：35 分。
- 相关经历与职责：30 分。
- 项目或行业证据：15 分。
- 教育及证书：10 分。
- JD 关键词覆盖：10 分。

权重只是初始假设，需要用测试样例校准。硬性条件缺失可以增加风险提示或设置总分上限，但必须把规则展示给用户。

**完成标准**

- 相同结构化输入得到相同规则分数。
- 总分等于分项分的明确组合，范围始终为 0～100。
- 每个已匹配项能指出简历证据，每个缺失项能指出对应 JD 要求。
- AI 只参与同义技能、职责语义等模糊判断，不可覆盖硬性数学规则。
- 至少覆盖“高度匹配、部分匹配、明显不匹配、空字段”测试。

## 阶段 7：Agent 设计

**学习目标**

- 理解 Agent 不是一次长 Prompt，而是“状态 + 任务步骤 + 工具 + 决策 + 停止条件”。
- 理解工作流与自由规划 Agent 的区别。
- 理解工具参数校验、幂等性、失败恢复和可观测性。

**需要你写**

- 一个明确状态对象：用户目标、简历 ID、JD ID、当前步骤和中间结果。
- 工具定义：读取简历、分析 JD、计算匹配、生成优化建议、生成话术。
- 固定工作流编排器，以及每一步的输入输出 Schema。
- 执行日志、失败状态、重试策略和停止条件。

**完成标准**

- Agent 能在给定简历和 JD 后按步骤完成一次分析。
- 任一步失败都可定位，不会无限重试或重复写入。
- 工具只能访问当前用户有权限的数据。
- 能说明为什么本项目初期采用确定性工作流，而不是让模型完全自由规划。

## 阶段 8：Chrome 插件

**学习目标**

- 理解 Manifest V3、content script、popup、service worker 和消息传递。
- 理解网页 DOM 易变化、站点适配器和最小权限原则。
- 理解浏览器扩展与后端的鉴权、CORS、隐私和用户确认。

**需要你写**

- `manifest.json` 和最小 popup。
- 针对一个测试页面或一个目标网站的 JD 提取器。
- content script 与 popup/background 的消息通信。
- 将提取文本预览后，由用户确认发送到后端。

**完成标准**

- 插件能在指定页面提取岗位标题、公司和 JD 文本。
- DOM 提取失败时给出提示，并允许用户手动编辑。
- 未经确认不上传页面内容。
- 权限范围最小，不读取无关网站。
- 网站结构改变时，仅需修改对应适配器，而不是重写整个插件。

---

## 第五部分：每个阶段应该真正理解的内容

### 阶段 1：系统边界与通信

你要能画出一次按钮点击从 Vue 到 FastAPI 再返回页面的路径；理解 URL、方法、状态码、请求体和响应体分别表达什么；知道环境配置为什么不能硬编码。重点不是记启动命令，而是建立“两个独立程序通过协议协作”的模型。

### 阶段 2：身份、数据与信任

你要理解数据库约束为什么比页面提示可靠，密码哈希为什么不可逆，JWT 为什么是签名而不是加密，以及服务端为何必须在每次受保护请求中重新验证身份和数据归属。还要理解 401 与 403 的差别。

### 阶段 3：文件是特殊输入

文件不仅是一个表单字段，它可能很大、名称恶意、内容伪装或传输中断。你要理解“扩展名、MIME 类型、文件签名”各能证明什么，以及数据库写入和磁盘写入如何避免一边成功一边失败。

### 阶段 4：解析不等于理解

PDF 解析只负责尽可能提取字符，AI 分析才负责把字符映射为经历、技能等语义结构。你要清楚原始文件、抽取文本、结构化结果三层数据不能混为一谈，这样以后才能重新解析、纠错和升级模型。

### 阶段 5：大模型是不稳定依赖

你要把模型视为可能超时、限流、输出格式错误、产生幻觉的外部服务。Prompt 是一种可版本化的程序输入，但不是绝对约束。可靠性来自输入清洗、明确结构、Schema 校验、失败处理、测试和人工确认的组合。

### 阶段 6：分数是一套产品规则

匹配分不是客观真理。你要能解释每个维度、权重和证据来源，理解关键词命中不等于能力匹配。好的评分系统应该可重复、可审计，并允许用户看到“为什么是这个分”，而不是只追求看似聪明的结果。

### 阶段 7：Agent 是受约束的执行系统

你要理解什么时候用固定流程，什么时候才需要模型规划。对求职分析这种步骤明确、涉及隐私的任务，先用确定性状态机更可靠。工具需要最小能力、严格参数和权限检查；Agent 必须有终止条件及人工确认点。

### 阶段 8：扩展运行在别人的页面里

网页 DOM 不是稳定 API，选择器可能随时失效。你要理解隔离执行环境、消息传递、站点权限与内容安全策略。尤其要把“辅助获取岗位”和“替用户自动行动”分开，任何对外操作都需要明确授权。

### 贯穿所有阶段的能力

- API 设计：资源命名、状态码、校验和错误格式。
- 数据建模：事实存储、派生结果、关系和迁移。
- 安全意识：密钥、密码、文件、权限和隐私。
- 测试思维：正常路径、边界、失败路径和可重复性。
- 调试方法：复现、缩小范围、查看证据、提出假设、验证假设。
- 产品判断：先解决核心问题，不被“Agent”概念带偏。

---

## 第六部分：30 天学习计划

建议每天保留三段时间：概念学习、独立编码、复盘记录。每天的“检查问题”应先由你自己口头或书面回答。

| 天数 | 学习任务 | 编码任务 | 检查问题 |
|---|---|---|---|
| 第 1 天 | 理解前后端分离、HTTP 请求与响应 | 初始化 Git，建立前后端目录和 README | Vue 与 FastAPI 为什么是两个进程？ |
| 第 2 天 | 学习 Vue 组件、响应式状态和事件 | 创建首页和一个可复用组件 | `ref` 中的数据变化为什么会更新页面？ |
| 第 3 天 | 学习 FastAPI 路由、类型提示和 Pydantic | 编写 `/health` 与一个简单 JSON 接口 | 路径参数、查询参数、请求体有什么区别？ |
| 第 4 天 | 学习 Axios、异步请求、CORS | 前端调用 `/health`，展示加载与错误状态 | 请求失败发生在浏览器、网络还是后端，如何区分？ |
| 第 5 天 | 学习配置、虚拟环境和依赖管理 | 整理 `.gitignore`、`.env.example`、启动说明 | 哪些内容能提交 Git，哪些绝不能？ |
| 第 6 天 | 学习关系型数据库、表、主键、唯一约束 | 配置 SQLite 和 SQLAlchemy，建立 User Model | ORM Model 与数据库表是什么关系？ |
| 第 7 天 | 学习 Pydantic Schema 和数据验证 | 编写用户创建与返回 Schema | 为什么不能直接把 ORM 用户对象全部返回？ |
| 第 8 天 | 学习密码哈希、盐和验证 | 完成注册接口和重复用户处理 | 为什么不能加密后再保存密码？哈希有什么不同？ |
| 第 9 天 | 学习 JWT 结构、签名、过期时间 | 完成登录、Token 创建与验证 | JWT 能否隐藏其中的信息？签名保护了什么？ |
| 第 10 天 | 学习 Vue Router、Pinia 和路由守卫 | 完成登录注册页面与认证状态 | 前端路由守卫为什么不能代替后端鉴权？ |
| 第 11 天 | 学习 multipart 表单和文件流 | 建立简历上传接口与上传组件 | 文件上传为何不用普通 JSON？ |
| 第 12 天 | 学习文件校验和安全存储 | 增加大小、格式、随机文件名和归属校验 | 原始文件名为什么不适合作为磁盘路径？ |
| 第 13 天 | 学习事务与失败清理 | 保存 Resume 记录，处理文件/数据库失败 | 如果文件成功但数据库失败，会发生什么？ |
| 第 14 天 | 学习 PDF 文本层和页面结构 | 用 PDF 库逐页提取文本 | 为什么有些 PDF 看得见文字却提取不到？ |
| 第 15 天 | 学习异常分类与测试样本 | 处理空、损坏、加密 PDF 并写测试 | 哪些错误应该返回给用户，哪些只写日志？ |
| 第 16 天 | 学习大模型 API、Token、超时和限流 | 用最小脚本验证 DeepSeek 调用，再封装客户端 | 外部 AI 服务失败时系统如何保持可理解？ |
| 第 17 天 | 学习 Prompt 的组成与结构化输出 | 设计简历分析 Prompt 和输出 Schema | Prompt 要怎样减少虚构而非只要求“准确”？ |
| 第 18 天 | 学习数据清洗与 Schema 校验 | 完成简历结构化分析流程 | AI 返回缺字段或非法 JSON 时怎么办？ |
| 第 19 天 | 学习 JD 的职责、必备项、加分项分类 | 设计 JD 分析 Prompt、Schema 与接口 | “熟悉”和“精通”是否应该被同等处理？ |
| 第 20 天 | 学习 mock、依赖替换和成本控制 | 为 AI 服务编写不调用真实 API 的测试 | 为什么测试不应依赖模型每次给相同回答？ |
| 第 21 天 | 学习集合、归一化和同义词 | 写技能标准化和精确匹配纯函数 | 大小写、别名和技能层级如何影响命中？ |
| 第 22 天 | 学习权重、分项分和硬性约束 | 实现第一版确定性评分函数 | 总分如何由分项组成？是否能手算验证？ |
| 第 23 天 | 学习语义匹配与证据链 | 用 AI 辅助判断模糊职责，但保留证据 | 模型判断与规则冲突时由谁决定？为什么？ |
| 第 24 天 | 学习边界测试与评分校准 | 编写四类固定匹配测试并调整权重 | 权重调整依据是直觉还是测试样本？ |
| 第 25 天 | 学习信息层级与报告呈现 | 完成评分卡、缺口和建议列表页面 | 用户首先需要看到分数，还是行动建议？ |
| 第 26 天 | 学习事实约束的文本改写 | 实现简历表达优化候选和前后对照 | 如何证明改写没有加入原简历不存在的事实？ |
| 第 27 天 | 学习语气、长度和个性化约束 | 生成并展示 BOSS 打招呼话术 | 话术中的每个个性化信息来自哪里？ |
| 第 28 天 | 学习状态机、工具和停止条件 | 画 Agent 流程图，写固定工作流编排器 | 哪一步真的需要模型决策，哪一步不需要？ |
| 第 29 天 | 学习 Manifest V3 和扩展消息机制 | 建立插件骨架，在测试页提取并预览 JD | content script、popup、background 各运行在哪里？ |
| 第 30 天 | 学习集成测试、安全复盘与技术债 | 跑通完整流程，修复问题，更新 README | 如果明天上线，最大的安全与可靠性风险是什么？ |

30 天的目标不是完成商业产品，而是完成一个你能解释、能调试、能继续扩展的 MVP。如果某一天的检查问题答不清楚，应暂停加功能，先补齐理解。

---

## 第七部分：开发规则

以后陪你开发时，我们遵守以下协作规则：

1. **不直接给完整答案。** 我不会一次性生成完整项目、完整模块或替你完成阶段作业。
2. **先解释原理。** 开始编码前，我会先说明目标、数据流、关键概念和常见错误。
3. **由你先写。** 我会把任务切成足够小的步骤，给接口契约、伪代码、提示或测试目标，再由你实现。
4. **写完再审查。** 你提交代码后，我会从正确性、结构、安全、边界条件和可读性几个方面检查。
5. **错误要解释原因。** 不只给修复代码；先帮助你复现、定位根因，并说明如何验证修复。
6. **一次只推进一个小目标。** 每次改动都应可运行、可验证，避免同时引入过多未知问题。
7. **先保证事实与安全。** 不允许 AI 虚构经历；不暴露密钥；不忽略用户数据归属和简历隐私。
8. **重要逻辑必须能测试。** 匹配算法优先写成纯函数，AI 调用应可替换，关键失败路径需要测试。
9. **你要能讲明白代码。** 如果某段代码能运行但你不能解释，我们把它视为尚未学会。
10. **每阶段先验收再继续。** 只有完成阶段标准并回答检查问题后，才进入下一阶段。

### 每次辅导的固定节奏

1. 你说明当前目标，并贴出你已写的代码或错误信息。
2. 我用问题确认你的理解，解释本次涉及的核心原理。
3. 我给出一个小任务、接口约束、伪代码或检查清单。
4. 你独立实现并运行。
5. 我审查代码，指出问题的证据、原因和修改方向。
6. 你完成修改，我们用测试或实际请求验收。

### 推荐的第一步

下一次从“阶段 1，第 1 天”开始：你先用自己的话画出或描述 Vue、FastAPI、SQLite 和 DeepSeek 之间的数据流，然后只初始化最小前后端项目。此时不要创建用户系统、AI 服务或 Agent 代码。
